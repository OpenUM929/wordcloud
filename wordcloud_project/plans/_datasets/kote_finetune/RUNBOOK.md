# hr-kote-finetune — 데이터셋 누적 RUNBOOK (상시 운영)

> ⚠️ **이 문서는 "계획서"가 아니다. 완료(DN) 개념이 없다.** 데이터가 들어올 때마다 반복 수행하는 **상시 절차 + 누적 로그**다.
> 설계(스키마·택소노미·보안): `../../2026/0617_05_kote-finetune-data/0617_05_kote-finetune-data.md` · 폴더 규약: [`README.md`](README.md)
> 핵심 가치: **긍↔부 오분류 방지.** positive gold는 정식 스트림에 826 적립 완료(2026-06-30, D5) — 다음 공백 = **gold 증강**(미판정 후보 검토·weak 승격, 아래 §누적 로그 참조).

---

## §0. 내 역할 — 핵심 엔지니어 (데이터셋 작업 전용 위임)

> 본 RUNBOOK이 트리거되는 모든 데이터셋 작업(감정/리더십 분석·강화, CSV 도착, 어노테이션, export/분할, 택소노미 갱신)에서 Claude는 **핵심 엔지니어**로 일한다. 이 역할은 아래 가드레일을 **덮어쓰지 못하고 품는다**.

**책임지는 것**
- 데이터셋 설계·택소노미·파인튜닝 전략의 기술 판단을 **주도**하고, 근거·트레이드오프를 먼저 제시한다.
- **긍↔부 0 오분류**와 데이터 무결성(append-only·비식별화)을 지키는 최종 기술 책임을 진다.
- 결정을 미루지 않되, **되돌리기 어려운 일**(스키마 파괴적 변경, 백본/대그룹 경계 변경, gold 대량 확정, 모델 학습 착수)은 "권고 + 선택지"로 올린다.

**그대로 지키는 가드레일 (역할이 못 덮음 — §2 불변 원칙·제약과 동일)**
- **추측 분류 금지** — 분류는 데이터 군집(TRAIT_TREE §6) 후에만. 빈 leaf는 grouped 유지.
- **append-only** — 기존 행 수정/삭제 금지, 정정은 동일 `id` 신규 리비전.
- **비식별화·dev 반출 금지** — 가명화 텍스트만, `src_hash`, 내부망 전용.
- **서버 무단 실행 금지 / dev 배치 불가·CSV만 / O(n)**(1.9만 규모).
- **사용자 고유 결정**(범위·예산·배포·신규 라벨 채택)은 **선점하지 않고 에스컬레이션**.

**작업 방식**
- 추측보다 **코드·데이터 확인 우선**. 못 박힌 사실은 재유도하지 않음.
- 완료/실패/생략을 **정직하게 보고**(과장·자축 금지). 핵심가치 위반 가능성은 즉시 표면화.

---

## §1. 트리거 (언제 이 RUNBOOK을 펴는가)

다음 중 **하나라도** 해당하면 반드시 본 RUNBOOK의 §2 체크리스트를 수행한다.

- 감정어/리더십 **분석·알고리즘 강화 작업**을 진행할 때 (검토 1회 = gold 확정 1회로 겸함).
- 취득 코퍼스 **CSV가 새로 도착**했을 때 (`data/*.csv` 반입).
- `acquired_sentences`에 **신규 행이 적재**되었을 때.

> CLAUDE.md "📦 학습 데이터셋 누적 지침"이 이 RUNBOOK의 나침반 진입점이다.

---

## §2. 데이터 도착 시 체크리스트 (매회 반복)

### §2-0. 단일 자기실행 명령 (먼저 이것부터 — 재량 개입 차단)

> ⚠️ **데이터 빌드를 임의로 건너뛰지 말 것.** 과거 1회, 범위를 "규칙 트랙 한정"으로 줄여 데이터셋 빌드를 생략한 사고가 있었다(입력 36만인데 기록 0). 이를 막기 위해 1~6단계를 **한 명령**으로 묶었다. 데이터가 오면 **반드시 먼저 이 명령을 돌린다.**

```
cd wordcloud_project/plans/_datasets/kote_finetune/scripts
python dataset_pipeline.py --in <드롭파일.jsonl|csv> [--date YYMMDD]
```

이 한 명령이 **반입 → 절분리(혼합극성 분해) → 이중 약지도(감정 override + 리더십 weak LF) → 비식별화 → append-only 스냅샷 → 패턴 마이닝 → 회귀 게이트 → 실행 요약**을 자동 수행한다(O(n)·서버/GPU 불요).

**기계검증 FAIL 게이트(자동 — 사람 판단 불요):**
- `입력 행수 > 0` 인데 `기록 레코드 = 0` → **FAIL**(빌드 누락. "규칙 트랙 한정"으로 0 기록 금지).
- 회귀 스위트(긍↔부 0) 실패 → **FAIL**.
- PASS/FAIL은 종료코드 + `result/pipeline_run_<date>.md`에 기록. **FAIL이면 §누적 로그에 '완료'로 적지 않는다.**

**자동(빌드) vs 사람(escalation) 경계 — 명문화:**
- **자동(절대 생략 금지)**: 위 1~6단계 = 약지도 데이터셋 빌드 + 패턴 후보 발굴.
- **사람(escalation에서만)**: gold **대량 확정**(3~4단계 confirmed), 신규 trait/대그룹 채택, 택소노미 파괴적 변경, 파인튜닝 착수. → 빌드를 멈추지 말고, 이 항목만 "권고+선택지"로 올린다.

> 스크립트: `scripts/dataset_pipeline.py`(오케스트레이터) · `scripts/leadership_lf.py`(리더십 weak LF) · `scripts/mine_patterns.py`(규칙·패턴 발굴) · `leadership/trait_tree.json`(택소노미 — 코드 아닌 데이터). 절 분리 = `src/modules/text_preprocessing.split_clauses`(production `split_sentences` 불변).

### §2-1. 단계 상세 (위 명령이 자동 수행)

| 단계 | 작업 | 도구/위치 | 산출 |
|------|------|-----------|------|
| 1. 반입 | CSV 업로드 → `acquired_sentences` 적재 | `acquired_data.html` "데이터 가져오기" → `POST /api/perspective/acquired-sentences/import` (`perspective_service.import_acquired_sentences_csv`) | 적재 행 |
| 2. 약지도 사전라벨 | KoTE top3 + 발동 규칙/리더십 극성 자동 부여 | `perspective_service.refine_acquired_row` (내보내기/검토 화면에서 재계산) | weak_sentiment, applied_rule |
| 3. 사람 검토(gold) | 우선순위 큐로 `sentiment_gold`(+선택 `emotions_gold`/`leadership_gold`) 확정 | `acquired_data.html` 검토 뷰 | `review_status=confirmed` |
| 4. 스트림 append | **confirmed gold만** 정식 스트림에 append-only 기록 | `emotion/emotion.jsonl`, `leadership/leadership.jsonl` | gold 누적 |
| **5. 규칙 재마이닝** | **신규 데이터로 케이스바이케이스 규칙도 함께 강화**: ① deferred 규칙(혼합극성 등, [`0617_01`](../../2026/0617_01_emotion-rule-mining/0617_01_emotion-rule-mining.md) §0-A) 표본 충족 여부 재확인 ② 신규 오분류 패턴(`rule_hurt`·저마진·검토 피드백)에서 표지/분기 도출 → `hr_context_lexicon`(`POSITIVE_MARKERS`/`NEGATIVE_MARKERS`/분기)에 **additive append** ③ 회귀 재검증 통과 확인 | `src/modules/hr_context_lexicon.py`(append) + `0617_01/test/run_*_regression.py`, `test_leadership_polarity.py` | 신규 표지/분기 + 회귀 ✅ |
| **5.5 잔여 하드케이스 판정(케이스 트랙)** | **규칙으로 못 잡는 잔여**(희망형·양보·타인지칭·저마진)를 **판정 패킷**으로 추출 → AI(Claude)가 1건씩 판정 → `sentiment_corrections`에 **가명 키 in-place 삽입**. 규칙(대량)과 별개로 **케이스바이케이스 최대치**를 뽑는 트랙. needs_human은 사람 모달 큐로. | `judgment_packet_service.build_judgment_packet`/`apply_judgment_packet` · routes `POST /api/perspective/judgment/extract`·`/apply` · 입력원 `eval/validation_candidates_<date>.jsonl` | corrections 보정 + needs_human 큐 + (확정분) gold 승격 후보 |
| 6. 스냅샷·분할 재생성 | 비식별화 게이트 + 누수방지 분할 + 품질 리포트 | `python scripts/export_jsonl.py` → `python scripts/build_splits.py` | `*.jsonl`, `result/*_report_<date>.md` |
| 7. **누적 로그 갱신** | 본 RUNBOOK §누적 로그에 1행 추가 | (이 파일) | 진행/공백 가시화 |
| 8. 핵심가치 점검 | 긍↔부 오분류 0 + positive gold 확보 추세 확인 | `result/split_report_<date>.md` | 게이트 통과 |

> 🔁 **규칙 트랙도 상시 루프다.** 규칙 마이닝은 `0617_01`(DN)에서 끝난 게 아니라, **데이터가 올 때마다 5단계로 재실행**된다(표본 부족으로 보류했던 deferred 규칙이 충족되면 그때 추가). 규칙은 파인튜닝(D5) 후에도 **후처리 가드로 유지**(`0617_05` §9·§12) — 모델이 규칙을 대체하지 않는다. 단, **추측으로 표지를 늘리지 않는다**: 코퍼스 오분류 근거가 있을 때만 append(`0617_01` §5-2 원칙).

### §2-2. 방법 일반화 — 두 트랙 + 리더십도 같은 골격

> 데이터가 와도, 리더십으로 넘어가도 **동일 절차**가 돌도록 방법을 못 박는다. 이 표가 "우리가 하는 과정"의 정본 골격이다.

**같은 데이터에서 함께 자라는 두 트랙(매 도착 반복):**

| 트랙 | 무엇 | 어떻게 | 누가 | 산출 |
|------|------|--------|------|------|
| **A. 대량/체계**(자동) | 빈출·체계적 오분류 버킷을 한꺼번에 교정 | `dataset_pipeline.py`(약지도+규칙 재마이닝 §2-1 step5)·FAIL게이트·회귀 | system(자동) | weak 스냅샷 + additive 표지/분기 |
| **B. 케이스**(AI/사람) | 규칙으로 못 잡는 잔여를 **1건씩** 판정 | 판정 패킷(§2-1 step5.5) → AI 판정 → in-place 삽입 + gold 확정(step3) | AI(Claude)/사람 | corrections 보정 + confirmed gold |

- **원칙**: 먼저 A로 대량을 정리 → 남은 잔여만 B로 케이스 추격(B를 A보다 먼저 돌려 낭비 금지). 두 트랙 모두 **긍↔부 0** 게이트를 공유한다. A는 substring 함정 위험으로 **체계 패턴만**(추측 금지), B는 함정 없이 개별 판정 가능.

**리더십 트랙도 같은 골격으로 진행(감정 ↔ 리더십 대응):**

| 단계 | 감정 트랙 | 리더십 트랙(같은 형태) |
|------|-----------|------------------------|
| 약지도 | KoTE override → pos/neg/neu | `leadership_lf.build_leadership_candidates`(grouped 기본·micro 힌트·polarity 재게이트) |
| 대량 마이닝(A) | `hr_context_lexicon` 표지/분기 additive | `trait_tree.json` 택소노미(코드 아닌 데이터) — split-only 단조 세분화 |
| 케이스(B) | 판정 패킷(긍/부/중 1건씩) | 동일 패킷 골격 재사용(trait/극성 판정 슬롯) — **무지도 군집(D1) 후** 세부 승격 |
| gold 확정 | `sentiment_gold` confirmed | `leadership_gold` confirmed(대그룹 우선, 세부는 군집 근거 시) |
| 회귀 잠금 | 긍↔부 0 | 긍↔부(positive↔risk) 0 — 같은 게이트 |

- **차이는 "분류 발굴 방법"뿐**: 감정은 코퍼스 오분류 근거로 규칙 append, 리더십은 **무지도 군집 후에만** trait 승격(추측 금지 — 빈 leaf는 grouped 유지). 절차·게이트·gold 흐름·패킷은 **그대로 재사용**한다. → 리더십 데이터가 오면 본 §2 루프를 그대로 돌리고, 마이닝(A)만 군집으로 치환.

### 우선순위 검토 큐 (3단계, 전수 아님 — 고가치부터)
1. `rule_hurt`(보정이 정답을 틀린 행) — 즉시 검수.
2. 극성 불일치(`weak_sentiment` ↔ 사람 직관, 특히 부↔긍 경계).
3. 저마진 argmax(`|pos-neg|<0.05`) → 신규 분기 발동분 표본 감사.

### 불변 원칙 (매회 준수)
- **append-only**: 기존 행 수정/삭제 금지. 정정은 **동일 `id`의 신규 리비전 행**(최신 confirmed 채택).
- **개선 이력 보고(기간대별·역사 전량 보존)**: 파인튜닝/재학습/기능 변경/게이트 통과 때마다 [`result/IMPROVEMENT_HISTORY.md`](result/IMPROVEMENT_HISTORY.md)에 **기간 행 1개 append**(덮어쓰기·삭제 금지) + run 원지표는 `result/learning_curve_*.jsonl`(append). 기간별 상세 서술은 `result/status_YYMMDD.md`(단일 스냅샷을 덮어쓰지 말고 날짜별 신규). 즉 현황 보고서는 **최신 한 장이 아니라 기간대별 누적**이라 개선 역사가 전부 남는다.
- **비식별화**: `source_*_id` → `src_hash`, PII 정규식 감사로 적발 행 격리(§14-1). 가명화 미완 텍스트 기록 금지.
- **dev 반출 금지**: JSONL·원문은 내부망 전용. `plans/`는 배포 제외 폴더.
- **신규 감정/리더십 그룹**: 코퍼스 발굴 근거 있을 때만 추가(추측 금지).
- **규칙 additive·추적성**: `hr_context_lexicon` 표지/분기는 **append만**(기존 표지·rule_id·시그니처 불변), 신규 분기엔 rule_id 부여. 추가 전후 **회귀(`run_*_regression.py`) 통과 필수** — 긍↔부 오분류 0 유지.

---

## §누적 로그 (append — 매 도착마다 1행)

| 날짜 | 입력원 | 입력 건수 | 기록 건수 | PII 격리 | gold confirmed | positive gold | 신규 규칙/표지 | 분할(train/val/test) | 비고 |
|------|--------|-----------|-----------|----------|----------------|---------------|----------------|----------------------|------|
| 2026-06-17 | `data/acquired_sentences_20260617.csv` 등 | 722 | 713 | 9 | 0 (약지도만) | **0** | `hr_context_lexicon` negation 게이트(긍61/부11 근거) | 554 / 70 / 89 | 첫 스냅샷. `user_label==model_label`(gold 부재). **positive gold 확보가 학습 선결.** 혼합극성 규칙은 3/475로 보류(deferred). |
| 2026-06-22 | `batch_20260622_0.jsonl` (다면평가, KoTE 사전라벨 36.4만) | 363,728 | ⚠️ 측정만(빌드 누락 — 06-23 정정) | — | 0 | **0** | **감정보정 §2-5 재마이닝**: `positive_rescue` positive-negation 가드(`관심이 없다`類 부→긍 27 차단) + `euphemistic_negative` negation 인식(`보완이 필요하지 않으며` 긍→부 1 교정). additive·`perspective_service` | (미실행) | **파일 `y`/`s`/`e`=KoTE 출력, 정답 아님 → 직접 재판정**(감사 165문장). old→new 양방향 긍↔부 0(신규오류 0), 회귀 5종 통과. 감사 리포트 `result/sentiment_eval_260622.md`. ⚠️ **이 행은 규칙 측정만 하고 데이터셋 빌드를 생략한 사고** → §2-0 단일 명령·FAIL게이트 신설로 재발 차단, 06-23에 실제 빌드. |
| 2026-06-23 | `default/` 5배치(장점_3차=0622_0·단점_3차=0622_2·단점_1차=0623_1·장점_2차=0623_2·단점_2차=0623_3, `.csv`지만 JSONL 내용) | 741,228 | **756,688**(문장 739,918 + 절 16,770) | 1,310 | 0 | weak 468,241(미확정) | **단일 파이프라인 `dataset_pipeline.py`**: 폴더/다중파일·내용기반 JSONL감지·batch태그 중복제거 + `split_clauses` + 리더십 weak LF + 감정 override + 검증셋 추출. 이중 스트림·weak-only | (build_splits 대기) | **자기실행+FAIL게이트 PASS·회귀 ✅·긍↔부0.** 배치별 분포 정상(강점 95% 긍 / 약점). 규칙수정 2종(`소홀` 결함가드·`has_constructive_need` 건설적필요가드, 약점 부→긍 12,795 교정·긍→부 0) 반영. 검증셋 `eval/validation_candidates_260623.jsonl`(42,474). 스냅샷 `emotion/weak_export_260623.jsonl`. |
| 2026-06-23 | `data/23년_장점.csv` (장점, KoTE 사전라벨, batch_20260623_0) | 523,715 | 검증셋 734(오판 케이스, gold 687) | PII 게이트 적용 | 687 (검증 gold) | — | **`rule3_last_low` 긍→부 보강(0623_02)**: 역량표지 7종(`우수·탁월·능동·원만·신속·열성·공정`) + 접미부정 가드(`~지 않/못`) + 반어 부정문맥어(`갑질·이기적·편향·우위 이용`·`불공정`) + `no_weakness` 저신뢰 게이트. additive·`perspective_service` | (검증셋만) | **한 줄짜리 장점이 끝문장이라 rule3로 무조건 부정화(긍→부 866)**를 발견·교정. dev 전수 재현(s=KoTE, 23초). 검증셋 gold 일치 **0.6→94.5%**, **부→긍 0**(전수 diff `n→p` 291=전부 긍정). 회귀 6종 통과. `eval/validation_rule3_rescue_260623.jsonl`, `result/accuracy_trend_260623.md §4차`. |
| 2026-06-24 | `data/23_단점.csv` (단점, KoTE 사전라벨, batch_20260623_0) + `data/23년 단점_eval.csv`(판정 패킷 judge, 128,800) | 505,011 | 검증셋 2,131(fixed 1,525 + 잔여 606) | PII 게이트 적용 | 1,525 (rule_evidenced negative) | — | **단점 맥락 부→긍 가드(0623_03)** `has_improvement_request`: 희망형(했으면/면 좋겠)·요함/요망·보완\|개선 요청·곤란(어려움), trap가드(중요함·"보완점 없음"·"어려움 극복"). positive_rescue 게이트 전용(is_no_weakness 미주입). additive·`perspective_service` | (검증셋만) | **0623_02 장점수정의 단점 적대검증**에서 발견: positive_rescue가 역량표지를 단점 맥락서 과구제 → 부→긍 11,039(기존 10,855 + 0623_02 187). 가드로 **−2,237**, 양방향 긍↔부 0(장점 94.5% 불변·하드 83.9→84.9·무작위 91.4 불변·장점 진짜 긍정트랩 0). 잔여 ~8,800은 맨 역량명사구·과잉·반어(필드/파서 트랙, 검증셋 606행 추적). 회귀 7종 통과. `eval/validation_cons_rule3_260624.jsonl`, `result/accuracy_trend_260623.md §5차`. |
| 2026-07-08 | `data/batch_20260708_0.csv` (내부망 실배치 최종판정 y 포함, 86.9만) | 868,889 | **886,985**(문장 867,889 + 절 19,096) | 1,000 | 0 (weak-only) | weak 523,104(미확정) | (발굴만 — 규칙 미반영) | (미실행) | **회귀 게이트 🔴 FAIL**(`test_euphemistic_negative_respects_negation`: "보완이 필요하지 않으며 높은 평가…" 긍정 기대 vs 0702 무결점 정책변경 후 `no_weakness_neutral`=중립 — 긍→중 방향, 긍↔부 아님. 테스트-정책 불일치 해소 전까지 후속 런 전부 FAIL). **내부망 실행결과 감사**: 그룹준수 무응답96.0·무결점96.5·과잉호소94.8·노력필요98.2·추측형필요96.3·개선요청89.0% / **건강조언 45.2%**(배포본에 0702 `health_advice_neutral`·0706 wellbeing 수정 미반영 = 배포갭). 블라인드 180문장 대조 일치 90.0%·**긍↔부 1/180**(과로 경계례). dev 현재규칙 재현 대조 90.48% — 드리프트 82,759행, 특히 **y=부→dev=긍 16,031(positive_rescue 주도) 재배포 전 적대검증 필수**. 검증셋 `eval/validation_candidates_260708.jsonl`(74,894)·`result/pattern_mining_260708.md`·감사 `scripts/group_audit_260708.py`. |
| 2026-06-24 | `data/24년 장점.csv`(batch_20260624_1, 444,080) + `data/24년_단점.csv`(batch_20260624_0, 427,287) | 871,367 | **889,465**(문장 870,367 + 절 19,098) | 1,000 | 0 (약지도만) | weak 555,492(미확정) | (발굴만 — 규칙 미반영) | (검증셋만) | **자기실행 PASS·회귀 7종 ✅·긍↔부0(골든).** 단 **필드×판정 교차 감사**: 단점필드 **30.8%(131,566) 여전히 positive**(KoTE 31.4%에서 거의 안 줄음). 직접 재판정 → 부→긍 강후보 **30,613**(개선요청 표지, 트랩 제외). **핵심 구멍: 78%(24,019)가 `rule4_default`=KoTE 직접 긍정·무override** → 기존 개선요청 가드(positive_rescue 전용)가 **구조적으로 못 봄**. 긍→부(장점필드)도 짧은 긍정 트레이트 명사구로 존재(규모 gold 필요). **권고: 개선요청 게이트를 rule4_default 경로까지 승격(→neutral 강등, 트랩 보존) — 사용자 승인 후 additive+회귀.** 리포트 `result/sentiment_misclass_260624.md`·`result/pattern_mining_260624.md`. |

> **규칙 마이닝(§2-5, 2026-06-30) — 결핍·개선요청 rule4_default 누수 차단(0630_03)**: 06-24 기록한 "단점필드 30.8% positive·78%가 rule4_default 무override" 구멍을 닫음. 기존 결핍/개선요청 게이트는 `positive_rescue` 차단 전용이라 KoTE 긍정우세가 그대로 `rule4_default`로 긍정 통과하던 부→긍 누수. 신규 분기 **`improvement_request_neutral`**(pos>neg + 개선요청core/건설적필요/결핍 → **중립** 강등, 부정 미생성=긍↔부 0) + 트랩 가드(`필요 인물`=불가결 제외, `has_improvement_request`를 core/곤란 분리해 "어려움을 돕는" 긍정 트랩 제거). **전수 재생(weak_export_260624 870,367행): 단점 부→긍 24,384 수정·긍↔부 신규교차 0·장점 긍→중 583(0.13%, 다수 방어가능).** 검증 중 `필요 이상`(부→긍 3)·`필요+조사` 합성명사 가드(부→긍 1,031) 시도→전수 양방향이 적발→revert. 회귀 기존 6종+신규 6종 PASS. 판단: 사용자 제보 `보완 필요`=부정이나 긍↔부 0 우선·개선요청 화행=중립으로 **중립 채택**(완전 부정화는 후속). 계획서 `2026/0630_03_deficiency-framing-neutral`. → [[project_finetune_groups_are_speechacts]]·[[project_sentiment_core_value]].
> **규칙 마이닝(§2-5, 2026-06-30) — 결핍명사 substring 부정화 시도·폐기(0630_04)**: 0630_03이 닫은 `필요/소홀/개선요청` 외 잔존하던 결핍*명사*(`부족/미흡/결여/부재`) rule4_default 부→긍 누수를 *부정화*로 닫으려 시도. 신규 게이트 `has_deficiency_noun_critical`(negation·양보·조력·자기개선 가드) + `deficiency_noun_negative` 분기 + positive_rescue 차단 추가. **전수 재생(weak_export_260624 889,465행): 가드 2차 후에도 긍→부 263(장점 138) 잔존** → 긍↔부 0 불가 → **코드 전량 원복·Drop**. 원인: `부족/부재/미흡`은 술어가 극성을 정하는 **토픽 명사**(장점필드 138 위반=`부족한 점을 챙겨주심`·`부재 시 대행`·`부족함에도 솔선수범` 진짜 칭찬 / 단점 차단분에도 `부족한 부분 없음` 약점없음 오탐 다수). 필드조차 분리 못 함 → substring 불가. 중립 후퇴도 장점 진짜칭찬 138 훼손이라 미채택. **결론: 결핍명사 정확화는 극성표(술어 인식) 또는 파인튜닝(P6) 정공법으로만.** 계획서 `2026/0630_04_deficiency-noun-neg`·결과 `result/replay_result_260630.md`. → [[project_polarity_lexicon_field_skew]]·[[project_sentiment_core_value]].
> **규칙 마이닝(§2-5, 2026-07-02) — 요청형→부정 정책전환 + 무결점→중립 구조규칙(사용자 확정)**: q.txt 지문·사용자 4문항 근거. `perspective_service`: ① `improvement_request_neutral`→**`improvement_request_neg`**(개선요청/결핍=부정, 단 **강긍정 pos≥0.75 가드**로 "필요사항을 잘 확인함"류 긍→부 차단·중립보류) ② `no_weakness_neutral` KoTE숫자게이트 제거(pos≫neg여도 무결점=중립, 명시적강긍정 완벽/뛰어남 보류·`_NOWEAK_WINDOW` 8→12·'장점 없음' 무언급 추가) ③ `has_constructive_need` 창 5→8+쉼표절단(무결점 '없' 포착). **양방향 전수(weak_export_260624 870,367행): 긍↔부 전이 0(양방향)**·단점 중→부 22,031(요청→부 의도)·무결점 긍/부→중 다수. **시도·폐기**: has_constructive_need에 명사복합/부사 제외 → "필요성이 있다"·"필요할 것 같음"까지 풀어 부→긍 113 → revert(공유게이트 불가침, 가드는 출력분기에만). 잔여(긍↔부 아님): 무술어 폴리세미 단편→긍(필드의존=파인튜닝 몫)·KoTE중립우세 요청형→중(neutral_dominant 선발동, 안전). 검토큐 재생성+정리(`regen`+`cleanup_review_260702.py`: 필드복원·무응답2,396제거·L2+SKEL 2단dedup) **19,486→17,445**, 판정 551 보존. 결과 `result/misclass_analysis_260702.md §구조규칙 채택`. → [[project_field_signal_for_finetune]]·[[feedback_validate_both_pos_neg_corpora]]·[[project_sentiment_core_value]].
> **규칙 마이닝(§2-5, 2026-07-02) — reorder: 개선요청 부정화가 neutral_dominant에 선점되던 버그(0702_03)**: 위 07-02 정책전환 후에도 사용자 재지적("X 필요·소통 부족이 여전히 중립"). 진단: 미판정 polysemy 결핍/요청 마커 2,105건이 부정 아님, 그중 **1,683건을 `neutral_dominant`가 선점**(규칙 순서상 neutral_dominant가 improvement_request보다 앞 → KoTE 중립우세 개선요청이 부정화 전 중립화). 수정: `improvement_request`를 `neutral_dominant` **앞**으로, `no_response`/`no_weakness`는 그보다 **더 앞**으로 이동(순서: positive_rescue→negation_praise→no_response→no_weakness→improvement_request→neutral_dominant→…). **양방향 전수(870,367행): 긍↔부=0**·중립→부정 7,000(개선요청 회복)·부정→중립 56(pos≥0.75). **자기교정**: 1차 순서(improvement가 no_weakness보다 앞)에서 "보완 필요점은 별도로 없습니다"류 무결점 16,154건이 오부정(improvement 8자 negation창이 원거리 '없' 놓침)→no_weakness/no_response 재이동으로 16,154→1. **규칙불가 확정(파인튜닝行)**: `해야/시켜야 함` 의지형은 `positive_rescue∧해야` 945건이 거의 전부 진짜 긍정("해야 할 일을 잘 처리함")이라 규칙화 시 긍→부 대량 → field-polysemy=파인튜닝. 무술어 명사구("업무 열의 및 노력")도 짧은 칭찬 억제 위험 → 사람라벨. 큐 재생성+정제 **→11,863**(polysemy 3,497→2,149·polflip 6,133·neutral_boundary 3,581), 판정 849 보존. 백업 `_gold_backup/*.pre_reorder260702.jsonl`. → [[project_neutral_is_the_hard_class]]·[[project_field_signal_for_finetune]]·[[feedback_validate_both_pos_neg_corpora]].
> **규칙 마이닝(§2-5, 2026-07-02) — 약점 못찾음/오타없음 무결점 인식 + 관형필요 가드 폐기(0702_03)**: 사용자 제보("70% 결핍·10~20% 오타 보완필요없음"). 진단(실측 15,782): 약점명사 ∧ 표준negation('없/않/아니') 없음 ∧ 부정 = `"보완필요점 찾지/느끼지/발견하지 못함"`·`"보완필요사항 업음(오타)"` — 부정표지가 `못/미발견/찾기어려움/오타없음`이라 `has_constructive_need('필요')`가 개선요청 오발동→부정화. 수정: `_is_weakness_not_found`(약점명사+not-found표지, 혼합 시 부정보존) 신설, `is_no_weakness_declaration`가 개선요청 가드보다 **먼저** 인식(not-found 명사는 순수 약점명사만 — '장점' 제외로 "느끼지 못함이 장점" 칭찬 보호). **양방향 전수: 긍↔부=0**·부→중 6,097(못찾음 회복)·긍→중 63(비평가 무결점). 못찾음/오타 잔존 15,782→**15**. **시도·폐기(재확인)**: 사람=긍/규칙=부 19건(`필요시/필요할 경우/보완이 필요한` 관형·조건 필요 칭찬)의 긍→부를 막으려 `has_constructive_need`·`_has_improvement_request_core`에 조건형 제외 → **부→긍 72 유발**("X 필요할 때가 있음"=개선요청까지 풂, "필요시 X를 잘 해줌"=긍과 동일 표면형) → **전량 revert**(113 revert 동형, 공유게이트 불가침). 큐 재정제 **→11,858**(polysemy 2,160·polflip 6,134·neutral_boundary 3,564), 판정 1,222 보존(사람 긍↔부 충돌 272건은 gold로 보존, 파인튜닝 하드클래스). 백업 `_gold_backup/*.pre_notfound260702.jsonl`. → [[project_field_signal_for_finetune]]·[[feedback_validate_both_pos_neg_corpora]].
> **규칙 마이닝(§2-5, 2026-07-02) — 참고컬럼 정합 + 건강조언 중립 + pos가드 유지확정(0702_03)**: 사용자 제보("게시판 참고 판정이 규칙과 전혀 안맞아, 3개 연달아 틀림"). 진단: 게시판 `내 판정(참고)`=`ai_reference.polarity`에 regen이 **raw KoTE**(규칙이 교정하는 원본)를 넣어 당연히 규칙과 불일치 — 실제 `현규칙`은 대부분 정답(보완오타 '보안이 필요없음'=중·건강=중). 수정 3건: ① regen `ai_reference.polarity`를 **규칙결과**로 정합(KoTE는 reason 맥락으로만) → 혼란 제거. ② **`health_advice_neutral`**(사용자 정책: 개인 건강·음주·체력 조언은 업무평가 아님→중립, 건강term+조언marker 동시요구로 "체력 좋아 업무 잘함" 칭찬 보존) — 양방향 긍↔부=0·pos→중 490(전부 건강term, 오탐 0), 고빈도 "건강관리가 필요함" 흡수. ③ **pos≥0.75 강긍정 가드 유지확정**: `improvement_request_neutral` 8,768건 실측이 진짜칭찬(필요사항 잘 파악·필요시 지원)과 개선요청 혼재 → 가드 제거 시 칭찬 긍→부 대량 → 고KoTE-pos '필요'는 파인튜닝 몫, 중립이 안전(사람 부정 라벨=gold 보존). 큐 재정제 **→11,963**(polysemy 2,276·polflip 6,073·neutral_boundary 3,614), 판정 1,225 보존. 백업 `_gold_backup/*.pre_health260702.jsonl`. → [[project_neutral_is_the_hard_class]]·[[project_field_signal_for_finetune]].
> **규칙 마이닝(§2-5, 2026-07-02) — 노력+필요 개선요청 pos가드 예외(0702_03)**: 사용자 격앙 재지적("대부분이 부족하니 노력하라는 내용"). 원인: "X 노력이 필요함"이 KoTE에서 노력/적극성 강긍정(pos≥0.75)으로 채점돼 `improvement_request_neutral`이 중립화. 코퍼스 실측 `노력+필요` **3,640건(2,300 distinct) 칭찬 반례 0**(칭찬은 "노력함/아끼지 않음") → 폴리세미 아님. 수정: `_is_effort_needed`(노력 직후 8자창 '필요', '필요없' 제외) 신설, improvement 분기에서 `if pos>=0.75 and not _is_effort_needed()`로 pos가드 우회 → 부정 확정. **양방향 전수: 긍↔부=0**(pos→neg 0, "필요시 지원 아끼지 않음"·"조직에 꼭 필요함" 중립 보존)·중립화 8,768→7,780(**988 개선요청 부정 회복**). polysemy 큐 노력+필요 중립 잔존=0. 백업 `_gold_backup/*.pre_effort260702.jsonl`.
> **규칙 마이닝(§2-5, 2026-07-03) — 보안(보완오타)·확인못 무결점 + '너무' 폴리세미 판정(0702_03)**: 사용자 2문항. **문1**(보완필요없음 오타 잔존): ① `보안`=`보완` IME오타를 약점명사(`_NOWEAK_NOUNS`·`_NOWEAK_NOTFOUND_NOUNS`)에 추가("보안점 필요없음"·"보안필요상황없음"→중립) ② not-found 표지 `확인하지 못/확인 못/확인되지 않` 추가("보완이 필요한 점은 확인하지 못하였습니다") ③ `_is_weakness_not_found` 강부정 가드에서 관형 '필요한' 제외(`_has_strong_negative_nonadnominal` — '보완이 필요한'은 not-found *대상*). **모두 →중립만 산출=긍↔부 불가**(부→중/긍→중). 실측 `보안`(비보완) 무결점 신규 4,848행(표본 100% 오타, 진짜 보안결핍 0), 큐 `확인못` 비중립=0·`보안` 비중립=19(진짜 보안업무/담당자 또는 `보안해야함`=개선요청). **규칙불가 잔여**: `보안 필요할 만한 점이 없습니다`(필요~없 10자>창8, 공유게이트 `_NEED_NEG_WINDOW` 확대는 부→긍 위험이라 보류)·`보안해야함`(positive_rescue 선점, 공유 `has_improvement_request` 수정=양방향 위험)→파인튜닝. **문2**('너무'→부정?): **블랭킷 부정화 불가(긍↔부 정면 위반)**. 실측 너무 7,943건 중 장점필드 537/626(86%)·강긍정어공존 1,000건이 칭찬("너무 좋음/친절/열심히")—부정화 시 1,000+ 긍→부. '너무'는 필드/문맥의존 폴리세미=파인튜닝(과거 `_WISH_FORMS` 제외 이유 동일). 큐 재정제 **→11,671**(polysemy 2,133·polflip 6,066·neutral_boundary 3,472), 판정 1,237 보존. 백업 `_gold_backup/pre_260703_typofix/`. → [[project_field_signal_for_finetune]]·[[feedback_validate_both_pos_neg_corpora]]·[[project_sentiment_core_value]].
> **규칙 마이닝(§2-5, 2026-07-03) — 과잉호소·추측형필요·무결점 4수정 + 그룹감사 전환(0702_03)**: 사용자 예시(부정 '너무', 무결점 유사, 결핍 잔존). 4수정: ① **`_is_excess_complaint`**(너무/지나치게 + 실측 칭찬오염0 귀결마커: 오해·못따라·과함·피로·힘듬·곤란·부담스·명시'문제입니다'구·지나치게[절내 not-negated]) → 부정. 가드: 명시강긍정(뛰어남)·무결점(단점 찾기 힘듦)·대조문 제외. ② **`_is_speculative_need`**(필요한 것 같/필요한듯/필요해 보임 = 결핍 추정, 인재/불요/부정 제외) → 개선요청 부정, positive_rescue 차단+improvement 분기에 반영(관형 '필요한 [명사]' 칭찬과 표면분리라 부→긍 재발 없음). ③ **positive_rescue 게이트**에 무결점/추측형필요/과잉호소 제외 추가("개선사항 없음"→중·"필요한듯"→부, 긍→중/미상향). ④ **`_has_unnegated_other_negative_strict`**(관형 '부족한/미흡한'=not-found 대상 제외)로 "부족한점을 발견하지 못했습니다"→중립 + 무공백 약점명사 보강. **양방향 전수(HEAD→NEW 870,367행): 부→긍 0**·긍→부 344(전량 단점필드 과잉/개선요청=필드 정답; 진짜칭찬 위반 0 — 초기 발굴 위반 '지나치게 푸쉬하지 않음'·'어떠한 문제든 긍정적'·'단점 찾기 힘듦'·'지나치게 뛰어남'을 가드로 전량 차단 후 재검). **그룹감사 전환(사용자 생각의 전환)**: 개별문장 대신 그룹별 검증 하니스 `scripts/group_audit_260703.py` 신설 — 타깃6그룹(무응답91·무결점99·건강98·과잉호소99·노력필요94·추측형필요98%) 원하는값 달성, 개선요청 그룹 86%(미달14%=대조위임+pos≥0.75 강긍정가드+데이터노이즈=의도된 안전/정답, 규칙확대 시 긍↔부 위반). 결과 `result/group_audit_260703.md`. 큐 재정제 **→11,587**(polysemy 2,108·polflip 6,010·neutral_boundary 3,469), 판정 1,307 보존. 백업 `_gold_backup/pre_260703_excess/`. → [[project_field_signal_for_finetune]]·[[project_neutral_is_the_hard_class]]·[[feedback_validate_both_pos_neg_corpora]]·[[project_sentiment_core_value]].
> **검토큐 그룹재편성(§2-1 step4, 2026-07-03) — 패턴그룹×도메인 파일 분할**: 사용자 요청(그룹별로 묶어 묶음판단·새 그룹 허용·세분화 OK). disagreement-type 3파일(polysemy/polflip/neutral_boundary)을 **패턴그룹(1무응답·2무결점·3건강·4과잉호소·5노력필요·6추측형필요·7개선요청·8기타[라벨별]) × 도메인(소통·전문·적극·배려·리더십·정도부사·성향·근태·IT·정확성)** 29개 파일로 재발행(`scripts/split_by_group_260703.py`, 소형<40행 도메인은 __etc 접기, field·라벨·정규화텍스트 정렬, 각 행 `group` 태그, 판정 1,307 보존). **감정엔진 무변경=긍↔부 위험 없음**(검토 조직화 전용). 규칙성 균질 그룹(grp2 무결점 83/85중·grp4 과잉 22/22부·grp6 추측 24/24부)은 묶음 확인, 집중검토=grp7 개선요청(도메인별 comm254·drive161·expert98·etc511)·`8c_other_neu__etc`(2,810=최대 이질잔여, 다음 세부그룹 발굴 1순위). 데이터품질 이슈 발견: 일부 행 HTML엔티티 인코딩(`&#48379;완`=보완)으로 규칙 미검출→소스정제 후보. 지도 `result/group_files_index_260703.md`. 원본 백업 `_gold_backup/pre_groupsplit_260703/`. → [[feedback_audit_by_group_not_sentence]]·[[project_leadership_trait_ref]].
> **규칙 마이닝(§2-5, 2026-07-03) — 1단계 그룹검토 피드백 4버그 교정**: 사용자 grp2/grp5 검토서 발굴. ① **무결점+명시강긍정 부정화 버그**: "완벽한 업무처리로 보완사항 없습니다"·"보완필요점 없고 역량 뛰어남"이 `has_explicit_strong_positive` 가드로 no_weakness 중립화만 건너뛰고 fall-through→rule4_default가 KoTE neg 따라 **부정**. → `is_no_weakness ∧ explicit_positive`시 **긍정 확정**(`no_weakness_positive`). ② **노력+필요가 positive_rescue에 선점**: "노력이 더 필요한 보완점"(관형'필요한'이라 constructive_need 미발동)이 positive_rescue→**긍(부→긍 위반)**. → positive_rescue 게이트에 `not _is_effort_needed` 추가 + improvement 분기 조건에 `_is_effort_needed` 추가. ③ **_is_effort_needed 절경계 누수**: "노력, 필요한 네트워크 보유"(칭찬, 노력·필요 다른 절)가 8자창에 걸려 부정→ 노력~필요 사이 쉼표 있으면 제외. ④ **'이나' 명사나열 오인**: "소통이나 공감능력 필요"(A나 B=or)를 대조로 봐 개선요청 중립화(grp7 중립 42/53). → '이나'는 전역 대조엔 유지(양보 "초임차장이나 뛰어남" 긍정 보존)하되 **개선요청 차단용 `_IMPROVE_BLOCK_CONTRAST`에서만 제외**. ⑤ 건강 오타 '건겅' health term 추가. **양방향 전수(HEAD→NEW 870,367): 부→긍 19(전량 no_weakness_positive=진짜칭찬 교정)·긍→부 246(장점필드 3=혼합/개선요청내용/부담스, 진짜칭찬 위반 0)**. 파이프라인 판정보존 개선: regen `load_decisions`가 review/·백업canonical도 읽음(그룹재편 후 판정 유실 방지) → **1,815 보존**. 큐 재정제·재분할 **→11,634**(review/ 29파일). grp2 부정잔존 0·grp7 중립 53→45(잔여=진짜대조'으나/지만'+pos≥0.75 강긍정가드=의도된 안전). → [[project_sentiment_core_value]]·[[feedback_validate_both_pos_neg_corpora]]·[[project_neutral_is_the_hard_class]].
> **규칙 마이닝(§2-5, 2026-07-06) — 개인 심신안녕(업무무관) 중립 도메인 게이트**: 사용자 그룹검토 피드백("건강·보완필요점 정보가 부정인 이유 이해 안 됨"·"'상처 안받았으면'·'스트레스 받지 않았으면'·'쉬어가며 일했으면' 등 평가무관은 중립"). 진단(검토코퍼스 실측): 건강 부정 68(rule3_last_low 39·improvement_neg 28)·개인안녕 부정 32 — `is_health_advice`가 **건강term AND 좁은 마커** 요구라 '하셔야/끊어야/생각하세요/요청' 미탐→rule3/improvement로 부정화, 스트레스/휴식/상처 도메인은 **탐지기 부재**. 수정: **`is_personal_wellbeing_neutral`**(개인 도메인명사 ∧ 염려/바람/조언 마커) 신설, override **최우선 분기**(positive_rescue보다 먼저)로 `personal_wellbeing_neutral` 중립 확정. **긍↔부 구조적 0**(중립만 산출·비발동행 바이트동일). 정밀화 2라운드: 다의명사(사기·마음이) 제외·substring충돌(술을→기술을/전술을, 과로→성과로) 제외·칭찬형마커(관리·해소·받지않·신경) 제외·'생각하'→명령형만 → **긍→중 1,590→488**(잔여 전량 "적극적이나 건강우려" 혼합문=item-3 원칙상 방어가능, 진짜 단독칭찬 손실 0). **양방향 전수(weak_export_260624 889,465행): 발동 5,011 = 부→중 1,143(의도수정)·중→중 3,380·긍→중 488(혼합/방어).** 검토파일 re-run(`scripts/rerun_wellbeing_fix_260706.py`): 102발동·83교정(비중립→중립), positive 43건 전부 "열정적이나 건강우려" 혼합=사람판정 중립과 일치. **claude_judgment per-row 정밀화**(그룹째 도장→건강/안녕行은 중립·진짜 개선요청은 부정 유지, 사용자 "단편적" 지적 교정). ⚠️ DN 아님(Pre-Done): 라우트/엔진 변경분 **서버 재시작 후 실동작 검증** 필요. → [[project_sentiment_core_value]]·[[feedback_validate_both_pos_neg_corpora]]·[[feedback_audit_by_group_not_sentence]].
> **검토큐 자동 정리(§2-1 step4, 2026-07-06) — 무종결 단편→중립(라벨링 원칙①)**: 사용자 지시("8c에 서술어 없는 미완성 문장이 대량, 규칙대로 치우면 양이 급감"). `scripts/clear_fragments_260706.py`: 미판정 행을 분류(자모난타/HTML깨짐/완성문/극성단편/무종결단편) → **무종결 단편(서술어 없는 명사구 ∧ 부정·요청·긍정 표지 모두 없음) → neutral**, 자모난타→not_group. **긍↔부 안전**(중립/그룹아님만). ⚠️ **자기교정**: 1차에 8a(긍)·8b(부) 버킷까지 적용해 "탁월한 소통 능력"류 긍정속성 단편 3,771 오중립 → **backup서 전량 복원 후** ① `_POS` 긍정표지 제외 추가 ② 단편→중립을 **8c(중립버킷) 파일에만** 한정해 재적용. 결과: 중립 **1,295**·그룹아님 16(8c 미판정 급감, 예: `8c__etc` 2,088→1,165). `decision_source=auto_fragment/auto_jamo` 태그로 사람판정과 구분(gold 승격 시 자동분 별도취급). 잔여: 완성문 4,780·극성단편 2,731·HTML깨짐 86(별도 정제트랙). 백업 `_gold_backup/pre_fragment_cleanup_260706/`. → [[feedback_incomplete_fragment_neutral]]·[[feedback_no_group_stamp_per_row]].
> **전처리 노이즈 필터 엔진화(2026-07-06) — 자모 난타·깨진 HTML 엔티티**: 사용자 지적("데이터 정제를 스크립트가 아니라 알고리즘이 해야"). 위 큐정리(script)는 검토큐 gold 확정일 뿐 엔진이 노이즈를 못 걸렀다 → **`text_preprocessing.split_sentences`에 승격**(프로덕션·미래 1.9만 배치 자동 적용): ① 깨진 숫자 HTML 엔티티(`&#NNNN;`)는 **복원 불가**(코드포인트 손상, `html.unescape`→'볻'≠보·오프셋 +7/−112 제각각) → **제거만**, 가독 잔여는 보존("…감독하는 향이 강함"=부정 유지). ② 낱자모 비율 ≥0.5 또는 '가나다라마바사' = 키보드 노이즈 → 문장 제외(채점 안 함). **안전**: 정상문 출력 바이트동일(낱자모 비율 0)이라 긍↔부 무영향, 노이즈 제거는 오히려 잔여문 is_last 정합. ⚠️ 세그멘테이션 변경이라 **캐시 재분석(서버 재시작) 후 반영**·실동작 검증 전 Pre-Done. → [[project_desktop_only_no_mobile]].
> **정식 gold 적립(§2-1 step4 / D5, 2026-06-30) — "positive gold 0" 블로커 해소**: 진단 결과 positive gold는 *확정* 부재가 아니라 *적립* 누락이었다 — 사람 검토는 끝났으나(eval/ 4파일에 `human_decision` 1,977건, ai_reference/규칙과 19~64%만 일치=독립 판정) **정식 스트림 `emotion/emotion.jsonl`이 0행**(D5 미실행)이었다. `scripts/promote_gold.py`(append-only·id dedup·PII감사·비식별화 게이트)로 confirmed 1,976 적립(**positive 826·negative 397·neutral 753**, not_group 1·dup 1 제외, conflict 0·PII 0). emotions_gold에 hr_그룹(약점부재/개선요청/성장지향) 동반. ⚠️ eval 행에 직원ID 부재 → `src_hash=null`(직원단위 누수보호 N/A — baseline은 held-out이라 영향 제한, 필요 시 원데이터 조인으로 후속 복구). → **정식 positive gold 0 → 826.** 미판정 후보 `group_gold_candidates*`(1,518)는 0624_05 검토 UI로 추가 적립 대기. → [[project_field_signal_for_finetune]]·[[project_sentiment_core_value]].
> **gold 증강 검토큐 생성(B+C, 2026-06-30)**: `scripts/build_gold_review_260630.py`로 0624_05 UI용 검토큐 2종 생성(human_decision 공란, **독립 힌트 2개**=군집/필드 polarity + `human_label` ai_reference[이유 포함], 두 신호 불일치=고가치 대조). **(B) `eval/group_gold_review_260630.jsonl`** 미판정 후보 1,518(라벨러↔군집 일치 909/불일치 609). **(C) `eval/weak_positive_review_260630.jsonl`** weak positive 553,077 중 층화 899(단점∧긍 누수후보 / 장점 pos≥0.95 안전수확 / 저마진 경계 각 300). ai_reference는 JSON 정형(구 파일 surrogate 깨짐 교정). → 사람 검토(긍/부/중/그룹아님) 후 `promote_gold.py`로 정식 적립. **gold 확정은 사람(추측 분류 금지) — 큐만 제공.**
> **gold 긍↔부 오염 재감사(2026-06-30) — 파인튜닝 투입 전 안전검사**: `scripts/audit_gold_260630.py`(독립 스크린: human_label + 미부정 부정어/긍정표지) → 의심 88(부→긍 31·긍→부 57) **직접 재판정**. **위험 오염(부→긍·긍→부) 0건** — 88건 전부 ① HL 부분문자열 트랩(`고압`·`갈등 조정`·`부족한 점 개선`=진짜 긍정) ② 중립 경계(허용 방향)였다. 개별 재판정(사용자 결정)으로 **18건→neutral 정정**(발전지향·연성제안·칭찬우세 개선요청; 명시적 결여·과잉 `적극성 필요`·`과도`는 negative 유지=사용자 원의도). `apply_gold_corrections_260630.py`(백업+prev_gold/rev provenance). **정정 후 positive 822·neutral 771·negative 383, 긍↔부 0 불변.** 결론: gold 1,976 안전 — 2차 파인튜닝 투입 가능. 리포트 `result/gold_audit_260630.md`·감사기록 `eval/gold_corrections_260630.jsonl`. → [[project_sentiment_core_value]]·[[feedback_distrust_prelabels_reanalyze]].
> **AI 합의 silver 증강셋 빌드(2026-06-30) — human gold와 분리 계층**: gold 1,976이 작고 부정클래스 얇아(383), 다면평가 코퍼스(weak_export_260624 870,367)에서 **3신호 합의**(KoTE override == human_label == 필드 prior, 대조/양보 트랩 제외) 고정밀 구역만 추출. 가능량 471,750(pos 236k·neg 80k·neu 156k) 중 **균형 30,000(각 10k, 텍스트 중복제거·gold 중복제외·PII제외)** 채택 → `emotion/silver_consensus_260630.jsonl`. **별도 계층**(`label_source=ai_consensus`·`tier=silver`·`review_status=ai_auto`) — human gold 절대 비혼합, 학습 시 gold=앵커·테스트/silver=증강(가중치 분리). **표본 적대감사: 부→긍 0.00%·긍→부 0**(negative의 긍정표지 동반 1,387은 전부 진짜 부정=결여·개선요청 오탐, 무작위 15 확인). 한계: silver negative는 개선요청 neu/neg 경계 노이즈 포함(허용 방향, 안전). 하드존(불일치 45.8%=39.8만)은 **의도적 제외** — 거기는 사람 C큐 몫(silver≠사람검토 대체). `scripts/build_silver_260630.py`. → [[project_field_signal_for_finetune]]·[[project_finetune_groups_are_speechacts]].
> **객관성 측정 — 학습곡선(2026-06-30)**: gold held-out(baseline 398)에서 gold train 비율별 파인튜닝 → 정확도 **50%(790) 88.7% / 75%(1,184) 89.5% / 100%(1,579) 90.5%**, 긍↔부 오류 0~2. **단조 상승=데이터가 천장**(평탄화 없음 → 올바른 데이터 더 넣으면 오름). **단 silver 30k 증강은 정확도 90.5%→77.1% 추락·부→긍 1**(silver가 gold 20:1 압도 → 쉬운구역 분포쏠림 + 개선요청 neu/neg 경계노이즈가 사람테스트와 충돌). **→ 양(silver)≠개선, 질·하드샘플이 답**(사용자 직관 검증). `scripts/finetune_sentiment.py --frac/--silver`, `result/learning_curve_260630.jsonl`. ⚠️ finetune은 eval/ human_decision 직접 로드 → emotion.jsonl 감사정정(18건) 미반영(실 2차학습 전 배선 필요).
> **능동학습 1라운드 하드샘플 큐(2026-06-30) — 사용자 라벨링**: "실패 10%를 정답화해 재학습"이 최고 레버(1차 56→89.7도 불일치존 gold). 단 실패구역은 자동라벨이 자기오류 강화 → **사람 판정 전용**. 무라벨 코퍼스에서 실패 대리신호로 발굴(`scripts/build_hard_failure_queue_260630.py`, CPU·누수없음): **s1 부→긍 누수후보 250(★)·s2 긍→부 50·s3 불일치 150·s4 저마진 100 = 550** → `eval/hard_failure_review_260630.jsonl`(ai_reference 힌트·human_decision 공란). 사용자가 0624_05 UI로 긍/부/중 판정 → promote_gold 적립 → 재학습·측정(능동학습 루프). silver(쉬운구역)와 상보: 양은 silver, 정확도·핵심가치는 하드샘플.
> **능동학습 1라운드 결과(2026-06-30) — 진짜 병목=중립 규약 불일치**: 사용자가 하드샘플 550 직접 라벨(긍138·부308·중98·그룹아님6) → 정식 적립(emotion.jsonl 2,520, 사용자 라벨 정본·내 18 감사정정과 충돌분은 정정본 보존). 재학습 측정(baseline 398 held-out): **대조(gold 1,579) 90.2%·중립재현 0.647 vs +하드(2,123) 89.9%·중립재현 0.618** — 하드샘플이 baseline 미개선(긍↔부 양쪽 0 유지). 예측 diff: 회귀 11≈개선 10, **회귀 8/11이 gold=중립→긍/부**(`잘되었으면 좋겠다`→부·`적극적 마인드와 지식`→긍·`장점 보이지 않음`→부). **원인=데이터 양 아님, 중립경계 라벨규약 불일치**(사용자 하드규약 개선요청·희망=부정 ↔ baseline 옛규약 중립). 모델은 중립경계서 오류 맞바꿈. **→ 선결=중립 규약 1회 확정·전 gold 일관화 + 하드 held-out 테스트**(baseline은 다툼구역 과소표집=잘못된 자). 긍↔부는 전 구간 0=핵심가치 안전. `model_out_round1`·`model_out_ctrl`. → [[project_neutral_is_the_hard_class]]·[[project_finetune_data_ceiling]].
> **재정렬 + 필드 프리픽스 측정(2026-07-01)**: 사용자가 옛gold 중립경계 456 재판정(변경 39: 과긍정개선구 긍→중 19·놓친결여 중→부 13) → 정식스트림 반영(긍941·중876·부703). 개선요청은 blanket 아닌 **3분岐**(능동=긍/발전지향=중/결여=부, 사용자 판정)=문맥의존 실증. 필드프리픽스 실험(`finetune_field_exp_260701.py`, 정식스트림 학습·baseline+하드held-out): **필드無 baseline 91.2%/중0.712 → 필드有 93.2%/중0.712**(긍0.965→0.987·부0.937→0.968, 긍↔부0). **필드는 긍↔부 모호구(전문성 향상=[장/단]) 분리엔 효과(+2%p), 중립재현엔 무효**(0.712 불변). 하드held-out 89.8→88.9(108건 노이즈). **→ 필드프리픽스 채택가치 有, 그러나 남은 천장=중립 화행 인식**(필드·silver 다 무효). 다음: 중립화행 gold 확충 / 신규그룹 멀티라벨 헤드. → [[project_neutral_is_the_hard_class]]·[[project_field_signal_for_finetune]].
> **중립 화행 확충 결과(2026-07-01) — 결손 클래스 보강 성공**: 사용자가 중립큐 520 라벨(중185·부191·긍144) → **중립 185 복원**(raw 모델은 이 화행에 중립 0). 건강기원 16건 중립 정정·바람 3분류 규약(현재 업무결여 지적=부정 / 격려축원·개인안녕=중립) 확립. 정식 gold **3,040**(긍1,085·중1,077·부878, 균형). 재학습 측정(정식스트림·baseline+하드held-out): **하드존 중립재현 0.625→0.77~0.82**(+0.14~0.20)·하드 정확도 88.9→90.7%·baseline 92.4%·긍↔부 0. baseline 중립(~0.71)은 평평(대표셋 쉬운 중립은 천장, 확충효과는 하드 중립에서). 필드프리픽스는 중립gold가 주역되며 노이즈화. **→ 데이터 일관성+중립 화행 gold가 천장을 밀어올림. 중립은 아직 천장 아님(더 넣으면 여지).** 바람→긍정은 raw KoTE 문제였고 gold는 이미 교정됨(오적립 2건뿐, 방어가능). `scripts/build_neutral_queue_260701.py`·`finetune_field_exp_260701.py`. → [[project_neutral_is_the_hard_class]].
> **남은 차단(blocker)**: 정식 positive gold 822 확보+재감사 통과로 P6 진입 선결은 충족. 추가 강화 = ① 미판정 후보 1,518 사람 검토(B 큐) ② weak positive 표본 899 검토 승격(C 큐) → 둘 다 `promote_gold.py`로 적립. (리더십 trait gold·세부 승격은 군집(D1)+사람 후 — weak 후보는 grouped/hint로만 적재됨.)
> **규칙 마이닝(§2-5) — C-1 채택·A-1 폐기 + 큐 재생성(2026-07-02)**: polysemy 검토(사람 200판정) vs 현재엔진 125불일치 분석(`result/misclass_analysis_260702.md`) 후 사용자 승인 하 규칙 손봄. **C-1 채택**: `is_no_response`에 강긍정(pos≥0.6·pos>neg) 가드 추가 → 무응답 오탐(`모르는 부분에 대해 설명` pos0.98)이 진짜 긍정을 중립화하던 것 차단. 회귀 5종 통과·weak_export 전수 **긍↔부 0**·중립→긍 1,506 회복. **A-1 시도·폐기(revert)**: "neg≥0.6·neg>pos면 positive_rescue 보류"는 전수 긍→부 5,445 유발(표본: `학구열 매우 높음`·`동료의식 강함`·역량단편 중립→부) → KoTE 역량구 부정과다예측 탓, neg임계 차단 부적합([[project_polarity_lexicon_field_skew]] 0630_04 교훈 동일) → 원복. 근본해법=필드(A-2, 후보풀 field 부재로 재생성 선행). B(개선요청 부정화)=정책 미결. **큐 재생성** `scripts/regen_boundary_review_260702.py`(현재엔진 재계산+dedup+164판정 보존): 34,457→24,849행(≈28%↓). → [[project_sentiment_core_value]]·[[feedback_execute_plan_no_descope]].
> **성장 여지 분석 + 경계 검토큐 추출(2026-07-02)**: 사용자 질문(감정 군집 3개뿐/경계 gold 얇음/성장률·리스트) 대응. 진단: ① 군집·극성은 별개 축이라 "긍정적/부정적 성장지향"은 이미 (그룹×극성)로 표현, 3개 군집은 극성 손실 아님 ② gold 얇음은 데이터량 아닌 **사람 검토 처리량 병목**(성장지향 경계 후보 8,549 중 확정 43=0.5%) ③ 유일한 성장 여지 = **neutral recall 0.647→0.8대 추정**(pos/neg 이미 ~0.95, 긍↔부 test 0), 전체는 천장 근처라 드라마틱 점프 없음(정직 고지). **추출**: `scripts/extract_boundary_review_260702.py`로 `validation_candidates_260624`(51,724 미확정)에서 게시판 규약 jsonl 3종 배타분할 — `polysemy_review_260702`(13,528: 노력/너무/개선류 폴리세미) · `polflip_review_260702`(7,653: 긍↔부 반전) · `neutral_boundary_review_260702`(13,276: 중립경계), 합 34,457(low_margin 제외). 3파일 게시판 로드 검증 통과, `/group-review` 자동 노출. **다음=사람 그룹검토→`promote_gold.py` 적립→재학습 측정(§6 추정 검증).** 상세 보고서 `result/growth_analysis_260702.md`. → [[project_neutral_is_the_hard_class]]·[[project_finetune_data_ceiling]].
> **규칙 마이닝 3회차(§2-5) 완료(2026-06-23) — 최대 정확도 개선**: 무작위 분포 손판정(70문장)에서 **3분류 정확일치 77%로 낮은 주범 = "약점 없음" 선언("보완점 없음"·"단점 없습니다")이 부정 오분류**(부정 라벨의 23.8% = 38,196건)임을 발견. KoTE가 '보완/단점'만 보고 부정, override는 긍정표지 없어 미구제. 규칙 **`is_no_weakness_declaration` → `no_weakness_neutral`**(약점명사+negation, 혼합은 negation-aware 게이트로 부정 보존, neg≥pos일 때만 발동=긍정문 불변) 신설. **실측: 부정 160,238→122,945(37,293 교정), 무작위 정확도 77%→91%, 긍↔부 2 유지(불변·중립만 산출).** 회귀 골든 추가. 벤치마크 `eval/accuracy_benchmark_random_260623.jsonl`. 정확도 추이 `result/accuracy_trend_260623.md`.
> **규칙 마이닝 2회차(§2-5) 완료(2026-06-23, 약점 배치)**: 약점 배치 부→긍 고neg(≥0.6) 표본 감사 → **지배 패턴 = "[긍정표지]+필요/요구"**(경청 필요·소통이 필요함·자세가 필요함·책임감 요구됨 = "더 ~하면 좋겠다" 건설적 비판). negation 인식 헬퍼 **`has_constructive_need`** 신설(관형형 '필요한'·'필요하지 않'·'필요 없'·'불필요'·'고객 요구' 전부 trap 제외) → `positive_rescue` 차단. **실측: positive→negative 10,786 + →neutral 2,009 = 12,795 교정, 무작위 25 표본 긍→부 trap 0**(diff 전수 검증). 부→긍 flip 29,071→23,929. 회귀 골든 추가. ⚠️ substring 목록(STRONG_NEGATIVE_PHRASES) 시도분은 '필요한' 관형 trap·bare '필요' 미포착으로 **헬퍼(경계+negation 인식)로 대체**.
> **검증용 데이터셋(2026-06-23)**: prelabel(KoTE)↔규칙 불일치 = '평가가 틀렸/어려운' 케이스를 `eval/validation_candidates_260623.jsonl`(**42,474 후보**: 긍↔부 flip·중립경계·저마진, 양쪽 라벨 보존)로 분리 — 향후 회귀·정확도 측정 기준(held-out, 사람 확정 대기). 파이프라인 단계로 배선(매 실행 자동 생성). 요약 `result/validation_set_260623.md`.
> **규칙 마이닝 1회차(§2-5) 완료(2026-06-23)**: 발굴 리포트의 결핍명사 786·부→긍 후보를 **표본 감사**(전수 flip 2,863 중 무작위 25 직접 판정). **정직한 결과: 현 파이프라인은 이미 flip의 ~90%+ 정확** — "786"은 대부분 노이즈/기처리였고, 진짜 부→긍은 산발적 소수. additive 가드 **`has_unnegated_deficiency`(결함술어 `소홀`, negation 인식)** 신설 → `positive_rescue` 차단. **실측: 부→긍 2건 교정, 신규 긍→부 0**(diff 전수 검증). ⚠️ `무관심`은 시도했으나 `업무관심도`(긍정) 부분문자열 trap으로 **긍→부 4건 유발 → 즉시 revert**(diff가 적발). 교훈: 단축 한글 substring 가드는 trap-prone → 광범위 추가 금지, 토크나이저 경계 인식은 후속. 회귀 골든 추가(`test_positive_rescue` catch/trap). `개인적인 ...에만 관심` 배타성은 trap 밀집(자기개발/본인이 떠맡아)으로 **보류**(표본 더 필요).

> **8c 하드클래스 gold 적립 + 파인튜닝 테스트 배선(2026-07-06)**: 8c_other_neu 잔여물(규칙이 전부 중립으로 버린 잔여, 알고리즘 라벨 100% neutral)을 사람 판정으로 gold화. **출처 표식 정규화**: board `/group-review/save`가 사람 판정 시 `decision_source='human'` 각인(claude_auto 프리필과 분리 → gold 순수성), 게시판에 ✓사람/auto 배지 노출. **백필**: 8c review 파일에서 확실한 사람행(auto 파일 hd≠제안 177 + residue legacy_human 783 = **960**)에 `decision_source=human` 부여(backup `_gold_backup/pre_ds_backfill_260706/`). not_group 95 제외 → **사람 gold 865**(pos 200/neg 265/neu 400). **누수차단 분할**: 정규화 중복제거·baseline 겹침 제외 → `eval/gold_8c_train_260706.jsonl` 725 + `eval/gold_8c_test_260706.jsonl` 140(pos37/neg41/neu62), train∩test=0. `promote_gold`로 정식 스트림 적립(3040→**3905**, +865, backup `emotion.jsonl.bak_260706`). **finetune_sentiment 확장**: TRAIN에 8c gold 추가, TEST를 2셋(`baseline399` 회귀가드 + `8c_hard` 이번검증)으로, before(알고리즘)/after(모델) 각각 ★긍↔부 출력. 검증(무GPU): 문법·로드·누수 0 확인. **다음=사용자 승인 하 GPU 학습**(`python scripts/finetune_sentiment.py --out model_out_8c`) → 8c_hard 긍↔부 flip 표 확인 후 모델 내부망 반입. claude_auto 867 prefill은 gold 아님(train silver 후보). 검증표본: 네판정 vs 사용자 실제판정 83% 일치·긍↔부 flip 1/1044.

> **8c 파인튜닝 실동작 결과(2026-07-06) — 합격·내부망 반입가**: 사용자 승인 하 GPU 학습(`finetune_sentiment.py --out model_out_8c`) 완료. **baseline399 회귀가드**: before(알고리즘) 65.1%·★긍↔부 **10**(부→긍 10) → after(모델) 92.2%·★긍↔부 **0**(회귀 없음, 오히려 알고리즘 긍↔부 오류 완전 제거). **8c_hard 검증셋(n=140)**: before 44.3%·긍0·부0·중1.0(알고리즘은 정의상 전부 중립) → after **80.7%·★긍↔부 1**(부→긍 1/140)·재현율 긍0.865·부0.756·중0.806 — 모델이 알고리즘이 못 잡던 하드클래스 극성을 사람 판정대로 복원. **핵심가치 안전**: baseline 긍↔부 10→0, 8c_hard 잔여 1건(유일 위험방향=부→긍, 다음 능동학습 1순위 하드샘플). 두 세트 회귀 없음 → **`model_out_8c` 내부망 반입 승인**. 리포트 `result/finetune_eval_260706.json`. → [[project_neutral_is_the_hard_class]]·[[project_sentiment_core_value]].

> **8c_hard 오답 감사 + 테스트 gold 정화(2026-07-06)**: 파인튜닝 모델의 8c_hard 오답 27건 CPU 추론 덤프(`scripts/dump_8c_errors.py`, GPU 아님) → 게시판 재판정 큐 `eval/review/error_8c_relabel_260706.jsonl`로 사용자 재판정. **발견: 27 오답 중 5건은 모델이 아니라 gold가 틀렸음**(모델 예측과 사용자 재판정이 5/5 일치): `자원최대동원수행` 중→긍·`기술적측면보완` 중→부·`인사이동후첫해임` 부→중·`자신의업무점검` 긍→중·`상황변화인식구체화` 긍→중. 사용자 규약=문맥 비정상(모호·미완결) 지문은 전부 중립. **동기화**(`sync_relabel.py`, 백업 .bak): held-out `gold_8c_test` 5건 정정 + 소스 review 4파일 일관화 + emotion.jsonl append-only 리비전 5행. **재측정(모델 불변, 측정선만 정화)**: 8c_hard 오답 27→**22**·정확도 80.7→**84.3%**·★긍↔부 여전 1 — 상승분은 모델 개선 아닌 **gold 노이즈 제거로 실제 실력 노출**. 남은 22오답=긍↔부 1(`낯가림·조바심`→긍 오판, 유일 위험)+중립경계 21(중립→극성 11·극성→중립 10). **교훈: "정확도 상승"의 1레버는 데이터 양 아닌 테스트 gold 자체 정합성**([[feedback_distrust_prelabels_reanalyze]]). 실제 모델 향상은 재학습 필요=능동학습 2R(22오답 유사문 마이닝→라벨→train, test행은 train 금지=누수). → [[project_neutral_is_the_hard_class]].

> **능동학습 2R 큐 마이닝(2026-07-06)**: 미확정 8c 2,652건(claude_auto 프리필 867 + pending 1,785)에 `model_out_8c` CPU 추론 → 불확실성/오류위험 표본만 선별(`scripts/mine_active_r2_260706.py`). 누수제외(train/test/baseline 정규화 동일)·중복제거 후 후보에서 **버킷 A(긍↔부 방어) 150**(필드-극성 불일치=단점인데 pos/장점인데 neg, 확신 높은 순=flip 위험 큼) + **버킷 B(불확실성) 200**(최대 softmax 낮은 순) = **350** → `eval/review/active_r2_260706.jsonl`. 버킷 A가 실제 flip 포착: `민원응대시 자기감정 조절`(장점)→모델 부정(감정조절=긍정인데 flip)·`자기주장 강함`→부정 등. **주의: 8c 필드(장점/단점) 신뢰불가**(부정내용이 장점 태깅 다수)→내용 employer-lens 직접 판정. **[정정] 빈칸 큐 오류→prefill+escalation 재구성**: 처음엔 350건을 human_decision=null로 사용자에게 통째로 던졌으나 이는 지침 위반([[feedback_prefill_judgment_escalate_uncertain]]) — 우리 방식은 claude가 먼저 판정하고 불확실만 사람. 8c_auto 파일의 기존 `claude_judgment`(내 판정) 재활용해 model과 대조 분리(`scratchpad/split_r2.py`): **escalation(claude≠model) 153**(내 판정 prefill, 사용자 override만) + **silver(claude==model 합의) 102**(자동 train silver) + **내 미판정(8c_other_neu pending) 95**(내가 먼저 판정 후 2차 배치). escalation 153 중 **긍↔부 정면충돌 14**(내 긍정↔모델 부정 11+역 3)=핵심가치 최우선(예: `감정 티 안냄`장점 나긍/모델부·`큰 목소리`나부/모델긍·`탑다운 통보`나부/모델긍). 파일 `eval/review/active_r2_{escalation,silver,nojudge}_260706.jsonl`. 다음=사용자 escalation 판정→silver+확정분 gold_8c_train 병합→GPU 재학습→8c_hard 재측정. → [[feedback_ambiguous_trait_employer_lens]]·[[project_sentiment_core_value]]·[[feedback_prefill_judgment_escalate_uncertain]].
> **능동학습 2R — nojudge 95 사람 gold 적립 + 라벨링 규칙2개 추가(2026-07-06)**: 사용자가 게시판에서 nojudge 95건 전량 판정 완료(`decision_source=human`, 긍47·부24·중24, 빈칸0) → `gold_8c_train_260706.jsonl` 725→**820** + `emotion.jsonl` 3905→**4000** 적립(`scratchpad/merge_gold.py`, 누수0·중복0, 백업 `.bak_260706_*`). **신규 라벨링 규칙(사용자 명시, [[feedback_incomplete_fragment_neutral]] 규칙4·5)**: ④ 한 문장에 긍+부 요소 혼재 → 중립(예 "역할구분 명확(긍)+본인외 관심없음(부)"), ⑤ 서술 극성 불명확 → 중립(예 "큰 목소리를 갖고 있음"). **적용**: escalation 제 prefill에 규칙 재적용(`scratchpad/apply_rules_esc.py`) → 긍↔부 정면충돌 **14→12**(`큰 목소리`·`정해진 직무범위 내에서만 수행` 2건을 중립으로 접음). 잔여 12=단일극성 내용↔필드 충돌(employer-lens 확신)=사용자 adjudication 몫. **주의: ④는 [[feedback_ambiguous_trait_employer_lens]]의 "양가태도→긍정"과 별개** — 단일 극성 기울면 긍정, 명시적 혼재/불명확만 중립. 다음=사용자 escalation 153(긍↔부 12 우선) 판정. → [[feedback_incomplete_fragment_neutral]]·[[project_sentiment_core_value]].
> **능동학습 2R — 저품질 prefill 교정(escalation 153→74, 2026-07-06)**: 사용자 지적("escalation이 알고리즘과 매우 크리티컬하게 차이남"). 진단: 내 중립 prefill 133/153이 검증된 model_out_8c와 100% 어긋난 이유 = **escalation은 정의상 claude≠model 집합**인 데다 내 중립 상당수가 **기계적 무종결→중립(auto_fragment) 잔재**라 저품질. 방향분해: 긍↔부 10 + 내중립↔모델극성 131 + 내극성↔모델중립 10. 131건 employer-lens **per-row 재판정**(`scratchpad/rejudge131.py`, POS/NEG rec_id 명시목록·매칭오류0): **79건은 모델이 옳아**(감정기복→부·자격증보유→긍·핵심파악→긍…) claude==model → **silver 편입(102→181)**, 50건은 진짜 중립(무난·기질·사생활·맨명사구·혼재) escalation 잔류. **긍↔부 10→14로 정직히 증가**(앞선 14→10은 중립으로 덮은 착시) — 그중 **4건은 모델오류**(민원응대 감정조절→부·자기감정조절 능력상향→부·모르면질문→부·특별한방법 능력배양→부, 전부 긍정을 부정으로 뒤집음)=재학습 최우선 신호. escalation **153→74**(긍↔부14 우선). 교훈: 기계중립을 prefill로 쓰면 저품질이 큐 오염 → escalation 직전 employer-lens 재판정 필수([[feedback_prefill_judgment_escalate_uncertain]]). → [[project_neutral_is_the_hard_class]]·[[feedback_distrust_prelabels_reanalyze]].
> **능동학습 2R 완료 — 사용자 153 실판정 gold 적립 + 무서술어 우선 교정(2026-07-06)**: 사용자가 escalation 153 전량 board 판정 완료(decision_source=human, 긍61·부30·중62). 내 재판정 프리필과 대조 시 일치 94/153 — **정본은 사용자 human_decision**(내 프리필 무시). **무서술어→중립이 employer-lens보다 우선**임을 사용자 교정: 내가 "민원응대시 자기감정 조절"·"자기감정조절 능력상향"·"특별한 방법…능력 배양"을 감정조절=긍정으로 판정했으나 사용자는 **무서술어라 중립**(조절·상향·배양=행위명사≠극성명사, [[feedback_incomplete_fragment_neutral]] 우선순위 확정). 적립: gold_8c_train 820→**973**(+153), emotion.jsonl 4000→**4153**, 누수0·중복0(`scratchpad/merge153.py`, 백업 .bak_153). 이번 세션 누적 사람 gold = nojudge 95 + escalation 153 = **248**(train 725→973). 다음=사용자 승인 하 GPU 재학습(`finetune_sentiment.py --out model_out_8c`)→8c_hard 재측정. → [[project_sentiment_core_value]].
> **재학습 실동작 + 3R 큐(2026-07-06)**: 사용자 승인 GPU 재학습(+248 gold, train 973) 완료. **8c_hard 긍↔부 1→0**(노린 부→긍 flip 제거)·**부 재현율 0.756→0.878**·정확도 80.7→82.9%; baseline은 긍↔부 0→1(긍→부, 오타 "적그적")·92.2→91.5%. **총 긍↔부 1로 불변(위치이동)** — 소형 테스트(140) 천장 근처라 극적 변화 없음(사용자 관찰 일치). 오답감사(`dump_errors_both.py`): 잔여 대부분 **중→극성**(baseline 22+8c 13)=모델이 중립을 극성으로 과판정, 3R 표적=중립경계 확정. **3R(사용자 스케일지침 "최대한 많이 뽑고 네가 판단 후 모호만": [[feedback_prefill_judgment_escalate_uncertain]])**: pending 383 전량 model_out_8c 추론(`mine_r3.py`)→모호 71(저확신<0.85 or 필드-극성 flip위험) 내가 per-row 판정(`build_r3.py`)→**escalation 34**(모호/긍↔부만, 내판정 중30·긍3·부1, 긍↔부 1="전문지식 요하나 적극적 임함"장점 모델 neg오류) + **silver 349**(고확신312+내확정37). 파일 `eval/review/active_r3_{escalation,silver}_260706.jsonl`(human_decision=null, 미판정 노출). 다음=사용자 34 판정→gold 병합→재학습. → [[project_neutral_is_the_hard_class]]·[[feedback_ambiguous_trait_employer_lens]].
> **3R 판정 완료 + 4R "내 오판 표적"(2026-07-07)**: 3R 34 사용자 판정(부12·중11·긍11) gold 적립(train 973→**1007**, +282 세션누적). **내 판정 vs 사용자 일치 15/34** — 사용자는 중립경계에 극성 부여(소신·세세=긍). **4R은 사용자 지시("내가 제일 오판한 내용으로 만들어")대로 능동학습**: 세션 내 내오판 78건(claude≠user) 패턴분석 = ①성향/성격(중→극성 미부여) ②직무기능·전문서술(긍→중 과판정) ③무난→긍·소극→부 미부여. 이 패턴으로 silver 470 매칭→**199 새모델≠내예전판정**(190이 내중립→모델극성=내 과잉중립 확증)→저확신/flip 57 재검(`mine_r4/build_r4.py`, 사용자 학습규칙 라벨러)→**escalation 25**(긍↔부0, 17건이 내중립↔모델긍정=직무기능서술 경계) + silver 174. 파일 `eval/review/active_r4_{escalation,silver}_260707.jsonl`. 다음=사용자 25 판정→r3+r4=59 gold 한번에 재학습(플랜A). 핵심경계=직무기능서술(자격·스킬·교류) 긍/중. → [[feedback_prefill_judgment_escalate_uncertain]]·[[feedback_ambiguous_trait_employer_lens]].
> **4R 완료(199 전량 판정) + 경계규칙 확정(2026-07-07)**: 사용자가 escalation 25 + silver 174 = **199 전량 판정**(긍75·중57·부67), 내 prefill과 64 불일치. 확정 규칙([[feedback_incomplete_fragment_neutral]] 9~14): ⑨**무난(단독)→중립**("자기 역할만큼"), 무난+긍정어("바른 시각으로"·"열심히"·"강함")→긍정 ⑩**일반 서술형·직무루틴(민원응대·상호작용 유지·부점수리)→중립** ⑪**능동 "문제점 발견하여 보완한다"(장점)→긍정** vs bare "~보완"(단점 개선요청)→부정 ⑫자격증/전문능력 습득·보유→긍정(단 근속경력만→중립) ⑬조용/내성적 성격이 단점프레이밍→부정 ⑭양가 성향(관철·이타적)→중립. 적립 train 1007→**1206**(+199, 세션누적 +481), emotion +199, r3 "관철" 1건 중립 교정(point5). **사용자 관찰: 모델 여전히 무난/직무서술을 저확신 긍정 과판정** → 이번 무난/서술형 중립 57이 교정 신호. 다음=재학습(무난/서술 긍정과판정↓·긍↔부 회귀 확인). → [[project_neutral_is_the_hard_class]].
> **9차 천장이후 레버 L1~L3 실측(0708_02, 2026-07-08)**: ①**L2 모델수프 기각** — seed{42,43,44,46} 재현 저장(`ensemble_eval_r8 --save-seed`)+`model_soup_r14.py`(uniform/greedy). uniform이 seed45 대비 전슬라이스 열세(baseline .895<.915·c3 .705<.738)에 **c3 긍↔부 0→3 유발**, greedy(seed45+43)도 c3 −5.4pp. **부수 발견: 앙상블 +1~3pp는 seed 평균 대비였고 best-seed45 대비로는 이미 없음(c3 ens .732 < seed45 .738)** → "미회수 이득" 전제 소멸, seed45 유지 확정(무손실 종결). ②**L1 라벨 일관성 감사(`label_audit_cv_r13.py`)** — (a)표면형 충돌 **22그룹 68행**(동일문·동일field 라벨 갈림, 무보완선언±근거 계열 최다) (b)5-fold oof 일치율 **81.35%**, 불일치 742 중 **중립 gold 394(53%)**=중립 비일관 정량 확증, 고확신 의심 498(긍↔부 방향 53) (c)테스트 4슬라이스 의심 78(06-30 원칙 재정 이전 구라벨 잔존: 개선요청=중립·장점bareNP=중립 등 — **측정 눈금 자체가 구식**; seed45의 "8c 긍↔부 1"도 의심 라벨("업무 분장확실에 보임/")이 원인). 내 per-row prefill **304행**(정정 제안 146 = 중→긍69·중→부45·긍↔부 방향 12, escalate 93) → `eval/review/label_audit_{queue,prefill}_260708.jsonl`. **정정 반영은 사용자 확정 후**(append-only 리비전 → 재학습 A/B). 하위확신 340행은 원칙 확정 후 2차. ③**L3 T scaling 서빙 배선** — `hr_sentiment.predict_proba`(+모델동봉 `calibration.json` T=1.6, 8차 seed평균)·판정패킷 `model_ref`(선택 소비) 부착. argmax 결정 불변·폴백 규약 동일, 단위 11/11 PASS. → [[project_neutral_is_the_hard_class]]·[[feedback_prefill_judgment_escalate_uncertain]]·[[project_sentiment_core_value]].
> **재배포 전 정비 3종(q.txt ①②③, 2026-07-09)**: 배포갭 원인 확정 — health_advice_neutral(0702)·wellbeing(0706)은 **미커밋 워킹트리 전용**(git -S 0건)이고 내부망 반입본은 7/2 이전 빌드(루트 잔재 `wordcloud-source/` 6/25자, 현 빌드스크립트가 갱신하지 않는 구산출물 — 반입 트랩). ①회귀 게이트 해소: `_EXPLICIT_POSITIVE`에 '높은/높이/높게 평가' 추가(4R ⑫ 무보완+칭찬어=긍정) → `test_positive_rescue.py` 전체통과. ②**부→긍 16,031 적대검증**: positive_rescue 12,275가 전부 **요청표지 화행 미탐**("노력해야한다"·"바랍니다"·"~보완") → `_has_request_marker` 신설(rescue 게이트 차단+improvement 분기 편입, 혼합칭찬은 중립 라우팅) + 부정칭찬 창 8→14·'~않도록' 목적형 제외. 트랩 5종 실측 제거(문말'노력'=장점 행위서술 1,616 긍→부 → 철회, '전자제어'substring '자제', bare'지양'=행위서술, '해야될+명사' 관형절, '예의도 바람'). **양방향 A/B(868,889 전량, `rescue_guard_ab_260709.py`)**: 부→긍 16,031→**12,760**(−3,271), 신규 긍→부 15행 전수판정=13행 bare NP 보완(구라벨 오류·원칙상 정당)+경계 2, y 일치율 90.48→90.91%. 잔존 12.7k는 필드의존 bare NP·rule4(모델/필드 신호 몫). ③구분선 쓰레기(`garbage_line_neutral`, 한글·영숫자 0이면 중립). 구정책 테스트 3파일 갱신(0630_03 중립→0702_03 부정 정책·5튜플 모델경로·패킷 v1→v2), 규칙 스위트 **56/56 PASS**. `build_deploy.ps1` 재빌드 + zip 내 규칙 5종·VERSION.json(7/9) 검증 — **내부망 반입 대기**(모델폴더 `calibration.json` 동반 복사 필요). 산출물 `eval/rescue_adv_drift_260709.jsonl`·`result/{rescue_adv,rescue_guard_ab}_260709.md`. → [[project_sentiment_core_value]]·[[feedback_validate_both_pos_neg_corpora]]·[[project_field_signal_for_finetune]].

---

## §3. 진행 현황 한눈에

- ✅ 인프라: **단일 파이프라인 `dataset_pipeline.py`(자기실행+FAIL게이트)** + `export_jsonl.py` + `build_splits.py` + 비식별화/누수방지 게이트.
- ✅ 절 분리: `split_clauses`로 혼합극성 문장을 단일극성 절로 분해(데이터셋 빌드 전용, production 불변) → **혼합극성 deferred 해소 착수**.
- ✅ 리더십 weak LF: `trait_tree.json`(백본2+대그룹9+세부20) 기반 `build_leadership_candidates` 가동 — grouped 기본·micro 힌트·polarity 재게이트(긍↔부 0), weak-only. 택소노미 변경은 코드 아닌 JSON에서(node id 재정렬).
- ✅ 패턴 마이닝: `mine_patterns`로 데이터마다 규칙·패턴 후보 자동 발굴(RUNBOOK §2-5 자동화) → `result/pattern_mining_<date>.md`.
- 🟡 감정 스트림 군집(D1, 2026-06-24): `scripts/cluster_emotion.py`(Track A, 무모델·O(n))로 **KoTE 미피복 부분코퍼스 484,119(전체 56%)** 멀티뷰 군집(24) → 3대 화행 수렴. 신규그룹 ≤3 후보 **G1 약점부재 선언·G2 개선요청·G3 역량서술**(기본 극성 neutral/positive, 긍↔부0 보호). `result/emotion_clusters_260624.md`·`result/emotion_new_groups_260624.md`. 계획서 `2026/0624_04_emotion-clustering`. **D4 완료(G1+G2)**: G2 개선요청 48,218이 현규칙 긍8,496/부26,906/중12,816 **3분열=누수 정량확증**, neutral 그룹화로 닫힘. 대표 AI판정·검증통과. **D4·D5 완료**: 표본 확대(gold 1,318·baseline 1,500), needs_human 679에 내 판정 `ai_reference` 동봉(사람 대조), **전체 176,483 weak 전파**(neutral 166,334/positive 9,627칭찬형/negative 326). 통합 리포트 `result/finetune_progress_260624.md`. 잔여=택소노미 문서반영·needs_human 사람확인·D6(파인튜닝, 사용자결정). Track B 인코더 미설치 보류.
- ✅ 규칙 트랙(A. 대량): `hr_context_lexicon` negation 게이트 가동(긍↔부 오분류 0, 회귀 ✅). **상시 5단계로 데이터마다 재마이닝**.
- ✅ 케이스 트랙(B): 판정 패킷(`judgment_packet_service`, 추출→AI판정→in-place 삽입, 가명 안전) 가동 — 규칙 잔여를 1건씩 케이스 최대치로 추격(§2-1 step5.5·§2-2). 입력원 = `eval/validation_candidates_<date>.jsonl`. 테스트 `0617_01/test/test_judgment_packet.py`(5종 ✅).
- 🟡 대기(0617_05 §13 결정·🟡 승인): gold 컬럼 additive 마이그레이션 + `acquired_data.html` gold 확정 UI(P1).
- ✅ **정식 gold 적립(D5) 완료(2026-06-30)**: confirmed 1,976 → `emotion/emotion.jsonl`(positive **826**·negative 397·neutral 753). `scripts/promote_gold.py`(append-only·dedup·PII감사). **"positive gold 0" 블로커 해소.** 후속 증강 = 미판정 1,518 검토 + weak 승격.
- ✅ **P6 1차 파인튜닝 완료(2026-06-24)**: 사람 gold 1,978 확보(needs_human 679+g4 100+field_conflict 800+baseline 399). KoTE 베이스→3분류 파인튜닝(학습 gold 1,579·비순환, 테스트 baseline 398 held-out). **전(규칙) 56.0%·긍↔부오류 10 → 후 89.7%·긍↔부오류 0**. 사람 gold가 규칙 천장(라벨러 ~63%·부→긍 누수)을 넘김. `result/finetune_report_260624.md`·`scripts/finetune_sentiment.py`·`model_out/`. ⚠️ gold는 `eval/_gold_backup/`에 백업(재생성 사고 2회→가드·백업 도입). 다음: 신규 배치 일반화 검증·weak 증강·신규그룹 멀티라벨 헤드.

### 택소노미 기준선 (2026-06-18 확정)
- **감정 스트림**: KoTE 44 + HR신규 ≤3 = **~47** (멀티라벨). **신규 3종 확정(2026-06-24, 0624_04 군집·미탐색 심화, ≤3 상한 충족)**: ① `hr_no_weakness_declaration`(약점부재, neutral) ② `hr_improvement_request`(개선요청, neutral) ③ `hr_growth_orientation`(자기개발/학습지향, **positive**, 신규=기존 규칙 없음·순환 아님, C7 근거·리더십 T14 대응). ①② 화행+neutral로 긍↔부 0 보호, ③ 검증된 선별기(정밀도~90%). weak: ①②176,483 + ③14,020 전파. G3(역량서술)은 안심/신뢰 기피복으로 보류. 미탐색 심화 결과 **더 큰 숨은 그룹 없음 확인**(`emotion_residual_findings_260624.md`). → [[project_finetune_groups_are_speechacts]].
- **리더십 스트림**: 유형(trait)을 **안정 백본(positive/risk) + 합집합 대그룹 9 + split-only 단조 세분화**로 관리(희소 세부→대그룹 합집합 흡수, 데이터 성장 시 split 독립). 같은 게이트를 **사람(리더) 단위 유형 타이핑**에도 재사용. 현 6역량=거친 macro 1층. 외부 레포(OpenUM929/leadership)는 **스키마만 흡수·gold 비흡수**(코드북·약지도 LF·군집 가설·부정표지 마이닝 / 미래 리더십 전용모델 시드) — 활용 전략·정본 스냅샷 [`leadership/trait_library_ref.md`](leadership/trait_library_ref.md) §0, 택소노미 스펙 [`leadership/TRAIT_TREE.md`](leadership/TRAIT_TREE.md).

---

*본 문서는 상시 운영 RUNBOOK이다. "완료"로 닫지 않는다. 데이터가 들어올 때마다 §2를 반복한다: **A. 대량 규칙 재마이닝(자동) + B. 케이스 판정 패킷(AI/사람) + gold 확정** → §누적 로그. **두 트랙은 같은 데이터·같은 긍↔부 0 게이트에서 함께 자라며, 리더십 트랙도 동일 골격(§2-2)으로 진행한다.***
