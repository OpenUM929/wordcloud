# batch_20260709_0 배포 검증 리포트 (2026-07-10)

## 결론

**정상 적용 확정.** `data/batch_20260709_0.csv`(핸드오프 코퍼스)는 7/9 배포분
(소스 zip 7/9 09:53 빌드 + 7/8 재학습 모델 `620faad…`)으로 정확히 판정된 산출물이다.
내부망 무결성 검사 "일치"와 코퍼스 라벨 정합이 상호 확인됐다.

| 검증 항목 | 결과 |
|---|---|
| 파일 무결성 | 868,890행, JSON 파싱 오류 0, 레전드 1행 정상, 가명화 텍스트 |
| 라벨 산출 경로 | 파인튜닝 모델(USE_HR_SENTIMENT_MODEL) — 규칙 폴백 아님 |
| 모델 버전 | **7/8 재학습본(620faad) 확정** — 아래 프리픽스 합의 검증 |
| 내부망 디스크 모델 | VERSION.json 무결성 검사 일치(620faad) |

## 모델 버전 판별 (핵심 증거)

서빙은 문장에 필드 프리픽스(`'장점 평가: '`/`'단점 평가: '`)를 적용해 추론한다
(0707_01/0708_01 train/serve 정합). 코퍼스에는 행별 필드가 없으므로 **프리픽스 3변형
(무/장점/단점) 예측이 합의되는 부분집합**으로 판별했다 (stride 표본 2,996행).

| 모델 | 3변형 합의 부분집합 일치율 | 변형집합 포함율(상한) |
|---|---|---|
| **7/8본(620faad)** | **2,674/2,674 = 100.00%** | **100.00%** |
| 6/25 구본(951a315) | 2,135/2,256 = 94.64% | 95.59% |

→ 배치 라벨은 7/8본 예측과 완전 정합. 6/25 구본은 배제.

## 라벨 품질 관찰

- **전체 분포**: 긍정 489,883(56.4%) · 중립 222,053(25.6%) · 부정 156,953(18.1%)
- **개선요청 화행 = 부정 반영**: 개선요청·결핍 표지 + KoTE 긍정우세 37,241건 중
  부정 30,859(82.9%) / 중립 5,638 / 긍정 744. 구 규칙 엔진이라면 전량 중립
  (`improvement_request_neutral`)이었을 패턴 — "개선요청=부정" 사용자 재정이 모델에
  학습·반영됐다는 직접 증거.
- **긍정 744건은 모델의 정당한 판정**: "도움이 필요할 때 친절하게 도와주십니다" 류 —
  렉시콘('필요')이 오포착하는 진짜 긍정 문맥을 모델이 올바르게 긍정 유지.
- **전향적 기대 → 중립**: "성장할 것으로 기대됨"(KoTE pos 0.94) → 중립. 비평가적
  화행 중립 원칙 반영.
- **긍↔부 안전성**: 표본 기준 모델-라벨 간 긍↔부 교차 0(합의 부분집합), bare 대조
  시에도 3/2,996(0.1%) 수준.

## 검증 방법론 교훈 (오진 기록)

초기 대조에서 프리픽스 없이(bare) 재추론해 "모든 후보 모델과 92~96%만 일치"로
**"구모델 메모리 상주·재시작 필요" 오진**이 있었다. 실제 원인은 서빙 프리픽스 누락.

- **재추론 대조는 반드시 서빙 규약(필드 프리픽스) 동일 적용** — 필드 미상이면
  3변형 합의 부분집합으로 판별.
- bare 추론으로 관찰된 "7/8본이 명백 긍정 단문을 중립 처리" 현상도 프리픽스를 붙이면
  소멸(예: `'장점 평가: 일을 열심히 합니다'` → positive 확인). **bare 추론 결과로
  모델 품질을 평가하지 말 것.**
- 후속 개선: 이 사건을 계기로 버전 검증을 선언·디스크·메모리 3자 대조로 확장
  (`verify_integrity`, 2026-07-10 — 파일 교체 후 미재시작 상태를 모달·배지에서 탐지).

## 산출물

- 검증 스크립트(스크래치패드): `verify_batch_260709.py`(전수 불변식),
  `prefix_agreement.py`(프리픽스 합의 판별), `candidate_sweep.py`(15종 모델 스윕)
- 대상 파일: `data/batch_20260709_0.csv` — **7/8본 산출물로 유효**, 학습·분석 활용 가능
