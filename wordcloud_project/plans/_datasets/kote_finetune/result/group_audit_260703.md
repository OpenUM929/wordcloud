# 그룹 단위 분류 감사 (2026-07-03)

> 사용자 요청(생각의 전환): 문장 하나하나가 아니라 **패턴 그룹**이 프로그램에서 원하는 극성으로
> 나오는지 본다. 상시 하니스: `scripts/group_audit_260703.py` (규칙 수정 후 매번 재실행).
> 코퍼스 `emotion/weak_export_260624.jsonl` 870,367행 전수.

## 그룹 × 엔진라벨 (원하는값 대비 일치율)

| 그룹 | 건수 | 긍 | 부 | 중 | 원하는값 → 일치율 |
|------|-----:|---:|---:|---:|----|
| 1 무응답 | 17,375 | 1,559 | 1 | 15,815 | **중 → 91%** |
| 2 무결점(없음/못찾음/오타) | 158,817 | 1,974 | 236 | 156,607 | **중 → 99%** |
| 3 건강조언 | 3,261 | 65 | 14 | 3,182 | **중 → 98%** |
| 4 과잉호소(너무/지나치게+귀결) | 716 | 7 | 708 | 1 | **부 → 99%** |
| 5 노력+필요 | 3,463 | 188 | 3,265 | 10 | **부 → 94%** |
| 6 추측형필요(필요한듯/것같) | 2,043 | 41 | 2,001 | 1 | **부 → 98%** |
| 7 개선요청/결핍(핵심) | 61,239 | 654 | 52,695 | 7,890 | **부 → 86%** |
| 8 기타(무술어·역량단편) | 623,453 | 518,983 | 90,388 | 14,082 | — (참고) |

## 누수 원인 진단 (원하는값과 반대로 나온 행)

목표그룹의 "미달분"은 개별 오분류가 아니라 **구조적 3원인**으로 수렴한다:

1. **대조문(지만/으나/반면에) → KoTE 방향 위임** — 그룹5 누수의 99%, 그룹7 긍누수의 98%.
   `열정적이지만 개인 행복 챙기는 노력도 필요` / `잘 하고 있으나 좀 더 바람` 류 **혼합문**.
   개선요청 분기는 `not has_contrast` 조건이라 대조문을 대조규칙(rule1/2)에 넘기고, 이들은 대부분
   칭찬우세(긍) → 부로 강제하면 **긍→부 위반**. 위임이 정답.
2. **pos≥0.75 강긍정 가드 → 중** — 그룹7 미달분의 대부분(7,681). `필요사항을 정확하게 파악하는
   부서장`(칭찬)·`보완할 필요가 있는 것을 잘 알려주심`(칭찬)과 진짜 개선요청이 고KoTE-pos에서
   섞여 규칙 분리 불가 → 중립이 안전(부로 밀면 칭찬 긍→부). **파인튜닝 존**([[project_field_signal_for_finetune]]).
3. **데이터 노이즈** — 그룹1 긍누수의 93%가 `//////`·`ㄱㄱㄱ`·`~~~~` 등 placeholder 행이
   rule4_default→긍. 분류 로직 문제 아님(입력 쓰레기).

잔여 실오분류(개선요청인데 긍): `이나`/쉼표를 대조로 오인한 가짜대조 ~0.2%
(`멘토링이나 코칭이 필요`·`능력을 기를 필요가 있음`). has_contrastive 손질은 공유게이트라 보류,
사람 라벨 gold로.

## 결론

- **타깃 6개 그룹(무응답·무결점·건강·과잉호소·노력필요·추측형필요) = 91~99%** 원하는 값.
- **개선요청 그룹 = 86%**, 미달 14%는 (대조위임 + 강긍정가드 + 노이즈)로 **의도된 안전·정답**이
  대부분. 규칙으로 더 밀면 긍↔부 핵심가치를 깬다 → 나머지는 파인튜닝/사람 gold.
- 양방향 전수(HEAD→NEW): **부→긍 0**, 긍→부 344(전량 단점필드 과잉/개선요청=필드 정답,
  진짜 칭찬 위반 0 — 뛰어남/무결점/부정의부정/문제해결 가드로 차단).

## 재현
`python plans/_datasets/kote_finetune/scripts/group_audit_260703.py`
