# 완료 보고 — 취득 코퍼스 정제 후 CSV (2026-06-15 20:11)

## 구현 내역

| # | 파일 | 변경 |
|---|------|------|
| 1 | `src/services/perspective_service.py` | `_sentence_sentiment_override_explain()` 신규 — (score, rule_id) 반환. `sentence_sentiment_override()`는 시그니처 유지하며 이 함수에 위임(동작 보존). |
| 2 | `src/services/perspective_service.py` | `refine_acquired_row(row)` 신규 — context 재분할 텍스트 매칭으로 위치 확정, KoTE 재계산, 보정 규칙 재현, 비교 플래그 산출. |
| 3 | `src/services/perspective_service.py` | `export_acquired_sentences_refined_csv(mismatch_only)` 신규 — 24컬럼 정제 CSV(BOM 포함). |
| 4 | `src/routes/perspective_routes.py` | import 추가 + `GET /api/perspective/acquired-sentences/export-refined` 라우트 신규. |
| 5 | `web/templates/acquired_data.html` | "정제 CSV" 버튼 + `exportRefinedCsv()` 추가. 기존 "CSV보내기" 유지. |

## 테스트 (test/test_refine.py)

- `test_override_behavior_preserved` — 6개 입력에서 기존 float 반환 == explain[0] 동일. ✅
- `test_refine_row_fields` — "성실하지만 보고가 미흡합니다."(context 2문장): KoTE neg=0.9929 → raw=negative, `rule2_contrast_lasthigh` 발동, corrected=negative, total=2, is_last=True, flag 정합. ✅
- `test_export_empty_or_current` — 빈 코퍼스에서도 헤더 포함 CSV 생성. ✅

전체 통과. dev에 KoTE 모델 로드 확인됨.

## 레거시 보호 준수

- `sentence_sentiment_override` 시그니처·숫자 결과 불변(위임만). 기존 호출처(`_get_sentence_level_scores` 등) 무영향.
- DB 스키마·취득 캡처 흐름 무변경 → 기존 수집 데이터 호환.
- 나머지는 전부 additive(신규 함수/라우트/버튼).

## 후속

- 정제 CSV가 일정량 쌓이면 `rule_hurt`/`kote_vs_truth=wrong & applied_rule=rule4_default` 중심으로 규칙 마이닝(별도 계획).
