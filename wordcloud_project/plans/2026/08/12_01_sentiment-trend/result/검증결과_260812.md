# 검증 결과 — 연도별 긍정/부정 추이 그래프 (12_01_sentiment-trend)

> 최초 작성 2026-08-12 · 최종 개정 2026-08-12 (당일 제2차) — rev-260812-02
>
> | 날짜 | 변경 | 요약 |
> |------|------|------|
> | 2026-08-12 | 전체 | 최초 작성 (구현 검증) |
> | 2026-08-12 | §7 신설 | Material Tailwind 디자인 개정 재검증 |
> 대상 계획서: `wordcloud_project/plans/2026/08/12_01_sentiment-trend/12_01_sentiment-trend.md`
> 판정: **Pre-Done** — 코드·단위검증은 통과했으나 실화면 시나리오(T1~T11) 미수행이므로 `Done` 아님

---

## 1. 실행 환경·제약

| 항목 | 내용 |
|------|------|
| 서버 기동 | **하지 않음**. 사용자 허가 없이 서버를 실행하지 않는다는 규칙에 따라 Flask 앱은 `create_app()` 로 **빌드만** 하고 URL map 만 확인했다 |
| 데이터 | dev 환경에는 실배치 데이터가 없다(원데이터 내부망 전용). 따라서 **합성 데이터 + 문장점수 스텁**으로 검증했다 |
| 미검증 영역 | 실데이터 정합(T3 단어 지표 방향성 · T7 대량 일괄 · T11 교차검증)은 이 문서로 담보되지 않는다 |

## 2. 정적 검사

| 검사 | 명령 | 결과 |
|------|------|------|
| Python 문법 | `python -m py_compile src/services/perspective_service.py src/routes/perspective_routes.py src/services/gallery_db_service.py` | 통과 |
| 인라인 JS 문법 | 템플릿에서 `<script>` 추출 후 `node --check` (`perspective_test.html`, `deploy_gallery.html`) | 둘 다 통과 |
| 라우트 등록 | `create_app()` 후 `url_map` 조회 | `/api/perspective/matrix/save-graph` 등록 확인 |
| 기존 로직 무변경 | `git diff --numstat` | `perspective_service.py` **+402 / −0** (삭제 0줄 = 기존 함수 무수정) |

## 3. 단위·통합 검증 (34항목 전건 통과 · 실패 0)

원본 출력: `result/_run_trend_260812.txt`

### 3-1. `test/test_sentiment_trend.py` — 집계·렌더 (19항목)

| 항목 | 검증 내용 | 결과 |
|------|-----------|------|
| A | 선택 연도별 독립 집계, 연도 정렬 | 통과 |
| B | 중립 문장(점수 0)은 긍정·부정 어느 쪽에도 미포함 | 통과 |
| C | 중복 문장은 앞 80자 기준 1회만 계수(`save_to_deploy` 와 동일 규칙) | 통과 |
| D | 평가 없는 연도 → 계열값 `None` + `skipped_rows` 기록 | 통과 |
| E | 백분율 분모 = 긍정+부정, 연도별 합 100 / 수량 모드 원값 유지 / 감정 강도 합 | 통과 |
| F | 단어 기준 경계 `score >= 0` 긍정 · `< 0` 부정 (워드클라우드와 동일) | 통과 |
| G | 차트 PNG 생성(pct·count 각 1회), 빈 입력은 `False` 반환 | 통과 |

### 3-2. `test/test_trend_save_path.py` — 저장 경로·갤러리 (15항목)

| 항목 | 검증 내용 | 결과 |
|------|-----------|------|
| A | `outputs/배포/그래프/<이름_사번>_긍부정그래프.png` 로 저장 | 통과 |
| B | 파일명 앞부분이 제출용 저장의 `safe_name` 규칙과 동일(`홍길동_110110`) | 통과 |
| C | 반환 URL `/outputs/배포/그래프/....png?v=<timestamp>` | 통과 |
| D | 갤러리 등록 `source='graph'`, `images={'graph': url}` (스키마 변경 없음) | 통과 |
| E | 평가 0건 / 선택 연도에 데이터 없음 → 예외 아닌 `None` 반환 | 통과 |

> 실제 `outputs/` 를 오염시키지 않도록 `DEPLOY_OUTPUT_DIR`·`OUTPUTS_DIR_PATH` 를 임시 폴더로 치환하고 갤러리 DB 쓰기는 가로챘다.

## 4. 렌더 육안 확인

| 확인 항목 | 결과 |
|-----------|------|
| 한글 (제목·범례·축 라벨) | 정상 — 깨짐(□) 없음. Windows `malgun.ttf` 경로로 등록 |
| 계열 | 긍정 초록 `#28a745` 원형 마커 · 부정 빨강 `#dc3545` 삼각 마커, 2계열 |
| 값 라벨 | 각 점 위/아래에 값 표시(%, 수량) |
| 결측 연도 | 실선은 끊기고, 존재하는 점만 잇는 **점선 보조선**이 겹쳐 그려짐 |
| 수량 모드 눈금 | 정수 눈금(0·1·2)으로 표시 — `MaxNLocator(integer=True)` |
| 주석 | `분모 = 긍정+부정 (중립 제외)`, `값 없음: 2025` 하단 표기 |

## 5. 계획서에 없던 보완 2건

| # | 보완 | 사유 |
|---|------|------|
| 1 | 결측 연도 **점선 보조선** | 계획대로 `None` 처리만 하면 중간 연도가 비었을 때 양쪽 선이 모두 끊겨 "연도별로 이어지는 선"이라는 요구가 성립하지 않았다. 값이 없다는 사실(실선 끊김)은 유지하고 흐름만 점선으로 보완 |
| 2 | 수량 모드 **정수 눈금** | 문장 수 1·2 가 `0.25` 단위 눈금으로 렌더돼 건수 지표에 맞지 않았다 |

## 6. `Done` 승격에 필요한 잔여 작업

계획서 §6 의 T1~T11 을 실제 화면에서 수행해야 한다. 서버 실행은 사용자가 직접 한다.

```
cd D:/dev/wordcloud/wordcloud_project && python web/app.py
```

특히 아래 3건은 합성 데이터로 대체할 수 없다.

- **T3** — 단어 지표 값이 실제 워드클라우드의 긍/부정 단어 집합과 방향이 일치하는지
- **T7** — CSV 다건 일괄 시 진행률·실패 목록·소요시간
- **T11** — 문장 수 지표가 제출용 저장의 `positive_sentence_details` / `negative_sentence_details` 길이와 일치하는지

---

## 7. 디자인 개정 재검증 (제2차 — Material Tailwind)

계획서 §10 참조. 차트 렌더 함수(`_save_trend_chart_to_path`) **하나만** 교체했고 집계·저장 경로·라우트·화면·판정 로직은 건드리지 않았다.

| 검사 | 명령 / 방법 | 결과 |
|------|-------------|------|
| Python 문법 | `python -m py_compile src/services/perspective_service.py` | 통과 |
| 회귀 — 기존 34항목 | `test_sentiment_trend.py` + `test_trend_save_path.py` 재실행 | **OK 34 / FAIL 0**, 두 스위트 모두 `전 항목 통과` (`_run_trend_260812_02.txt`) |
| 착수 전 원본 대비 | `diff backup/src/services/perspective_service.py ↔ 현재` | **삭제 0줄 / 추가 434줄** — 계획 착수 전 코드 무수정 유지 |
| 렌더 육안 확인 | `preview/` 4종 PNG 직접 확인 | 아래 표 |

| 미리보기 | 확인 내용 | 결과 |
|----------|-----------|------|
| `preview/01_pct_5y.png` | MT 룩(흰 카드·점선 그리드·축선 없음·회색 12px 라벨), 곡선 오버슈트 없음, 교차 지점 값 라벨 겹침 없음 | 정상 |
| `preview/02_pct_gap.png` | 결측 연도(2023) 실선 끊김 + **빈 구간만** 점선, 각주 `값 없음: 2023` | 정상 |
| `preview/03_count_5y.png` | 수량 모드 정수 눈금(0·4·8·12), 음수 눈금 없음 | 정상 |
| `preview/04_pct_2y.png` | 2점은 직선(PCHIP 미적용), 단어 기준 각주 표기 | 정상 |

**렌더 확인 중 발견해 고친 2건** — 두 계열이 교차하는 지점에서 값 라벨이 서로 겹치던 문제(위쪽 계열 값을 위로 배치하도록 수정), 범례가 하단 각주와 붙던 문제(축 높이·범례 위치 조정).

**이 개정으로 담보되지 않는 것**: 실화면에서 「그래프 저장」으로 생성한 PNG 가 `preview/` 와 동일한지(T12). 서버를 띄우지 않았으므로 렌더 함수 직접 호출 결과만 확인했다.
